---
phase: 01.3
slug: fix-pre-existing-phase1-self-test-yml-test-design-bugs-inser
status: ready
nyquist_compliant: true
wave_0_complete: true
created: 2026-04-27
updated: 2026-04-27
---

# Phase 01.3 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | vitest 4.x (existing — installed in Phase 02-00) |
| **Config file** | vitest.config.ts |
| **Quick run command** | `npx vitest run --reporter=dot` |
| **Full suite command** | `npx vitest run` |
| **Estimated runtime** | ~5 seconds (unit) — primary validation is the live `phase1-self-test.yml` ubuntu-latest run, not local unit tests |

---

## Sampling Rate

- **After every task commit:** Run `npx vitest run --reporter=dot` (locally) — fast confirmation that no production-code regressions in `src/index.ts` runDryRun
- **After every plan wave:** Run `npx vitest run` (full suite, locally)
- **Before `/gsd-verify-work`:** Full local suite green AND a fresh push to GitHub triggers `phase1-self-test.yml` and all six scenarios + the `self-test-hyphenated-input-env` regression job pass on ubuntu-latest
- **Max feedback latency:** ~5 s local, ~3-5 min remote (CI run)

> The authoritative validation surface for this phase is the **live ubuntu-latest run** of `.github/workflows/phase1-self-test.yml`. Local unit tests cannot reproduce the runner's step-header emission timing, the per-step `$GITHUB_STEP_SUMMARY` semantics, or the `::add-mask::` registration order — those were the blind spots that hid the bugs in the first place. Task 4 of `01.3-01-PLAN.md` is the live-CI gate.

---

## Per-Task Verification Map

| Task ID    | Plan | Wave | Requirement | Threat Ref       | Secure Behavior                                                                                                                                                              | Test Type    | Automated Command                                                                                                                                                                                                                | File Exists | Status     |
|------------|------|------|-------------|------------------|------------------------------------------------------------------------------------------------------------------------------------------------------------------------------|--------------|----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------|-------------|------------|
| 01.3-01-01 | 01   | 1    | TEST-01     | T-01.3-01        | runDryRun calls `core.setOutput('dryRunSummary', md)` exactly once with the same markdown string passed to `core.summary.addRaw`. Setoutput post-dates setSecret × 3.        | unit         | `npx tsc --noEmit && npx vitest run tests/unit/dispatcher-dry-run.test.ts --reporter=dot && npx vitest run --reporter=dot`                                                                                                       | ❌ W0 (new) | ⬜ pending |
| 01.3-01-02 | 01   | 1    | TEST-01     | T-01.3-02, T-01.3-05 | action.yml declares top-level `outputs.dry-run-summary` mapping `${{ steps.run-playwright-healer.outputs.dryRunSummary }}`; Step 6 carries `id: run-playwright-healer`; description includes "diagnostic / not stable across versions" disclaimer. Slug-consistency guard. | structural   | `npx tsc --noEmit && npx vitest run tests/unit/action-yml-structure.test.ts --reporter=dot && npx vitest run --reporter=dot`                                                                                                     | ❌ W0 (new) | ⬜ pending |
| 01.3-01-03 | 01   | 1    | TEST-01     | T-01.3-03, T-01.3-04 | Scenario 1 `::add-mask::` setup step precedes `uses: ./`; Scenarios 4+5 read `${{ steps.run.outputs.dry-run-summary }}` via `env: SUMMARY:` indirection (no `$GITHUB_STEP_SUMMARY`); Scenario 6 + verify-log-mask + Scenarios 2/3 unchanged. | YAML lint    | `node -e "const y=require('yaml');const fs=require('fs');const d=y.parse(fs.readFileSync('.github/workflows/phase1-self-test.yml','utf8'));if(!d.jobs['self-test-masking']\|\|!d.jobs['self-test-hyphenated-input-env'])process.exit(1);console.log('OK')" && npx tsc --noEmit && npx vitest run --reporter=dot` | ✅          | ⬜ pending |
| 01.3-01-04 | 01   | 1    | TEST-01     | (live CI bridge) | Live ubuntu-latest run of phase1-self-test.yml: all 7 jobs (Scenarios 1–6 + verify-log-mask Job B) green. The composite-action output bridge AND the `::add-mask::` ordering work end-to-end on the actual runner. | live-CI      | `npx tsc --noEmit && npx vitest run --reporter=dot && [ "$(gh run list --workflow phase1-self-test.yml --branch \"$(git rev-parse --abbrev-ref HEAD)\" --limit 1 --json conclusion -q '.[0].conclusion')" = "success" ]`        | n/a (CI)    | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

Existing infrastructure covers all phase requirements:

- [x] `vitest.config.ts` — installed in Phase 02-00
- [x] `tests/unit/` directory — exists
- [x] `tsx` runtime — already pinned via Phase 01.2
- [x] `yaml` library (used by `tests/unit/action-yml-structure.test.ts`) — already in `package.json` dependencies as `yaml@^2.8.3`

**Two NEW test files are introduced (not Wave 0 scaffolding — they're folded into Tasks 1 and 2 with `tdd="true"`):**

- `tests/unit/dispatcher-dry-run.test.ts` (created in Task 1)
- `tests/unit/action-yml-structure.test.ts` (created in Task 2)

No new framework or scaffolding required. The phase is YAML + 1-line TS + 1 action.yml `outputs:` block + 2 small vitest test files (~ 80 + 70 lines respectively).

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| All six scenarios + the `self-test-hyphenated-input-env` regression job green on a fresh ubuntu-latest run on a clean fork (no repo secrets) | TEST-01 | Live runner masking + `$GITHUB_STEP_SUMMARY` per-step semantics + step-header emission timing cannot be reproduced locally | Task 4 of `01.3-01-PLAN.md` automates this in yolo mode: pushes the branch, captures run id via `gh run list`, watches via `gh run watch --exit-status`, captures per-job conclusions JSON for SUMMARY |
| Scenario 1 Job B (verify-log-mask) actually fails when canary leaks past the runner masker (regression-test the regression-test) | TEST-01 (SC#2) | Requires deliberately breaking the new `::add-mask::` setup step or the `with:` shape and pushing — confirms the assertion has teeth | Optional one-off after this phase: temporarily comment out the `echo "::add-mask::<canary>"` line, push to a throwaway branch, confirm Job B fails; revert. NOT part of normal plan execution — sanity check only. |

---

## Validation Sign-Off

- [x] All tasks have `<automated>` verify or Wave 0 dependencies
- [x] Sampling continuity: no 3 consecutive tasks without automated verify (every task in `01.3-01-PLAN.md` has an `<automated>` command)
- [x] Wave 0 covers all MISSING references — existing infra + 2 new test files folded into Tasks 1 and 2
- [x] No watch-mode flags
- [x] Feedback latency < 300 s (CI run included)
- [x] `nyquist_compliant: true` set in frontmatter

**Approval:** ready (pending live-CI green from Task 4 — flip to `passed` upon SUMMARY commit)
