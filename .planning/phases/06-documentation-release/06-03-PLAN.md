---
phase: 06-documentation-release
plan_id: 06-03
plan: 3
type: execute
wave: 2
depends_on:
  - 06-01
  - 06-02
files_modified:
  - README.md
  - CHANGELOG.md
  - .github/workflows/security-lint.yml
autonomous: true
requirements:
  - PKG-05
  - DOC-01
  - DOC-03
  - DOC-04
  - DOC-05
task_count: 3
must_haves:
  truths:
    - "README.md has all 11 sections specified in D-05, including the Mermaid sequence diagram (DOC-01)"
    - "README Quick Start shows the 1-PR adoption path with references to docs/examples/gemini.yml"
    - "README §Prerequisites prominently documents trace: on / retain-on-failure, upload-artifact, and token requirements (DOC-03)"
    - "README §Token scopes explains GITHUB_TOKEN recursion guard in plain language (DOC-04)"
    - "CHANGELOG.md follows Keep a Changelog format with [Unreleased] section and draft v0.1.0 entries"
    - "README §Auto-merge prerequisites stub anchor preserved (P-06 option a)"
    - "security-lint.yml updated to exclude new documentation files from pull_request_target grep"
  artifacts:
    - path: "README.md"
      provides: "Full 11-section adoption surface"
      contains: "sequenceDiagram"
    - path: "CHANGELOG.md"
      provides: "Keep a Changelog [Unreleased] section with v0.1.0 draft entries"
      contains: "[Unreleased]"
    - path: ".github/workflows/security-lint.yml"
      provides: "Updated exclusion list for new docs files"
  key_links:
    - from: "README.md ##quick-start"
      to: "docs/examples/gemini.yml"
      via: "Quick Start links: 'Copy docs/examples/gemini.yml into your .github/workflows/'"
      verified: false
    - from: "README.md ##auto-merge-prerequisites"
      to: "docs/auto-merge.md"
      via: "Anchor stub preserved + redirect sentence added (P-06 option a)"
      verified: false
    - from: "CHANGELOG.md [Unreleased]"
      to: "[0.1.0] in plan 06-06"
      via: "[Unreleased] entries promoted to [0.1.0] at tag-day in plan 06-06"
      verified: false
---

<objective>
Author the main README (11 sections, D-05) and the CHANGELOG.md [Unreleased] section (D-07). Also update security-lint.yml to exclude the new documentation files from its pull_request_target grep. This plan does NOT create the docs/ companion files — those are in plan 06-04.

Purpose: README is the primary adoption surface for PKG-05 (SC#1: consumer adopts in one PR under 15 minutes). CHANGELOG.md [Unreleased] is the foundation for the v0.1.0 release in plan 06-06. Security-lint exclusion update prevents false positives on SECURITY.md containing legitimate references to security controls.

Output: Full README, CHANGELOG with [Unreleased] section, updated security-lint.yml.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-documentation-release/06-CONTEXT.md
@.planning/phases/06-documentation-release/06-RESEARCH.md
@.planning/phases/06-documentation-release/06-PATTERNS.md
@action.yml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write README.md — all 11 sections</name>

  <read_first>
    - 06-CONTEXT.md §D-05 — 11-section README structure (the specification)
    - 06-CONTEXT.md §Specifics — tone reference ("practical-detailed, code-snippet-first")
    - 06-RESEARCH.md §R-03 — Mermaid sequence diagram source (lift verbatim)
    - 06-RESEARCH.md §R-05 — provider cost/quality matrix (framing for §Switching providers)
    - 06-PATTERNS.md §README.md — what to lift verbatim vs what differs
    - README.md current content (16 lines) — the `## Auto-merge prerequisites` anchor at line 5 MUST be preserved
    - action.yml — canonical input surface for §Prerequisites and §Token scopes
  </read_first>

  <files>README.md</files>

  <action>
Replace README.md with the full 11-section document. Tone: practical-detailed, code-snippet-first, NOT marketing tone. Modeled on anthropics/claude-code-action README style.

**Critical constraints:**
- PRESERVE the `## Auto-merge prerequisites` anchor (currently at line 5). It must exist in the new README, now containing: "For the full prerequisites matrix, see [docs/auto-merge.md](docs/auto-merge.md)." (P-06 option a — zero code change to pr-writer.ts)
- The Mermaid sequence diagram in §Architecture is lifted verbatim from RESEARCH.md §R-03 — do not alter the diagram source
- Consumer-facing: no phase numbers, no GSD terminology, no .planning/ references
- All action inputs referenced must match action.yml exact input names (snake_case)

**Section structure (D-05):**

1. **What it is** — 1 paragraph describing the tool + 1-line value proposition. Include "What it does NOT do" callout: heals test code only, not app code.

2. **Quick start** — the 1-PR adoption path:
   - Step 1: Copy `docs/examples/gemini.yml` (or `docs/examples/github-models.yml`) into `.github/workflows/heal.yml`
   - Step 2: Copy `docs/examples/ingest.yml` snippet into your existing CI workflow
   - Step 3: Add secrets `GEMINI_API_KEY` and `HEALER_PAT` (or just `HEALER_PAT` for GitHub Models)
   - Step 4: Push — the ingest workflow will start collecting stats; dispatch happens automatically when thresholds are breached
   Note: link to `docs/examples/gemini.yml` as the recommended starting point

3. **Architecture** — include the Mermaid sequence diagram from RESEARCH.md §R-03 verbatim (inside ```mermaid code fence). Brief paragraph before the diagram explaining the two-workflow hybrid.

4. **Prerequisites** (DOC-03 — PROMINENT CALLOUT):
   - Playwright must have `trace: 'on'` or `retain-on-failure` configured in playwright.config.ts
   - Your existing CI workflow must have an `upload-artifact` step that uploads the Playwright report
   - `actions: write` permission OR a `HEALER_PAT` PAT (for cross-workflow dispatch)
   - Node 20+ on your CI runner (action uses Node 24 internally)

5. **Token scopes & why GITHUB_TOKEN doesn't work** (DOC-04):
   - Explain GitHub's recursion guard: GITHUB_TOKEN cannot trigger downstream CI on bot-opened PRs
   - Document required PAT scopes: `repo` (contents:write, pull-requests:write) + `models:read` (GitHub Models only)
   - For fine-grained PATs: `Contents: write` + `Pull requests: write` + `Issues: write` on consumer repo
   - Explain `healer_token` vs `api_key` input semantics

6. **Example workflows** (DOC-02):
   - Reference `docs/examples/gemini.yml` (Gemini 2.5 Flash — default, free tier, 1M context)
   - Reference `docs/examples/github-models.yml` (gpt-4.1 — single PAT covers both api_key + healer_token)
   - Reference `docs/examples/ingest.yml` (shared ingest snippet, paste into existing CI)

7. **Switching providers** — 1-section subsection. Summarize 4 providers + 1-line input diff per provider:
   - Gemini (recommended in v0.1.0): `provider: gemini`, `model: gemini-2.5-flash`, `api_key: ${{ secrets.GEMINI_API_KEY }}`
   - GitHub Models (recommended in v0.1.0): `provider: github`, `model: openai/gpt-4.1`, `api_key: ${{ secrets.HEALER_PAT }}`
   - Anthropic (preview — stub in v0.1.0): `provider: anthropic`, `model: claude-sonnet-4-6`, `api_key: ${{ secrets.ANTHROPIC_API_KEY }}`
   - Ollama (preview — stub in v0.1.0): `provider: ollama`, `model: llama3.1`, no api_key required (localhost)
   **NOTE TO EXECUTOR:** Anthropic and Ollama are throw-on-call stubs in v0.1.0 (see CLAUDE.md §Key architectural facts). The README MUST clearly mark them as "preview" or "coming soon" — do NOT list them as functional options. Gemini and GitHub Models are the two fully implemented providers. Mention the stub limitation once in prose before the provider table, e.g.: "Gemini and GitHub Models are fully supported in v0.1.0. Anthropic and Ollama adapters are in preview and not yet functional — use Gemini or GitHub Models for production use."

8. **Auto-merge** — 1-paragraph summary + link to docs/auto-merge.md. This section MUST include the `## Auto-merge prerequisites` anchor (H2 heading) to preserve the existing link target from pr-writer.ts core.warning text.

9. **Troubleshooting** — 5–7 entries covering:
   - Heal step OOM: increase runner memory or reduce max_turns
   - Agent fix rejected by diff-lint: see what forbidden patterns were triggered (job summary shows details)
   - Validation failures: post-fix pass rate too low; check test stability
   - PR not opening: check HEALER_PAT scopes; GITHUB_TOKEN won't work (see §Token scopes)
   - Auto-merge soft-fail: see docs/auto-merge.md §Soft-fail behavior matrix
   - Mermaid diagram not rendering: if the diagram shows as raw text, ensure you're on github.com (not a GHE instance that may lag)

10. **Roadmap** — 1 paragraph: what's coming in v0.1.x (live SC#2 auto-merge demo, more providers) and v0.2 (app-code fix capability, trace-aware confidence bands).

11. **Contributing & Security** — 1-line links: "See [CONTRIBUTING.md](CONTRIBUTING.md) for contribution guidelines. See [SECURITY.md](SECURITY.md) for vulnerability reporting."
  </action>

  <verification>
    <automated>grep -c "sequenceDiagram" README.md && grep -c "Auto-merge prerequisites" README.md && grep -c "docs/examples/gemini.yml" README.md && echo "README_STRUCTURE_OK"</automated>
    <human-verify>
      1. Read through the Quick Start section — verify a new consumer can understand all 4 steps without prior knowledge of the tool.
      2. Verify the Mermaid diagram renders correctly by pushing to a draft PR or checking mermaid.live with the diagram source. The diagram must have 8 lifelines and use `autonumber`.
      3. Verify the `## Auto-merge prerequisites` H2 anchor exists and links to `docs/auto-merge.md`.
      4. Check that §Token scopes explains the GITHUB_TOKEN recursion guard clearly — a developer unfamiliar with GitHub's recursion guard should understand why they need a PAT.
    </human-verify>
  </verification>

  <done>
    - README.md contains all 11 sections from D-05
    - `grep -c "sequenceDiagram" README.md` returns 1
    - `grep -c "Auto-merge prerequisites" README.md` returns at least 1
    - `grep -c "docs/examples/gemini.yml" README.md` returns at least 1
    - No phase numbers, GSD terminology, or .planning/ references in consumer-facing prose
    - All action input names match action.yml exact snake_case names
  </done>
</task>

<task type="auto">
  <name>Task 2: Create CHANGELOG.md with [Unreleased] section</name>

  <read_first>
    - 06-RESEARCH.md §R-06 — CHANGELOG v0.1.0 entry draft (lift and refine)
    - 06-PATTERNS.md §CHANGELOG.md — structural format (Keep a Changelog, not Conventional Commits)
    - 06-CONTEXT.md §D-07 — CHANGELOG specification + deferred items list
  </read_first>

  <files>CHANGELOG.md</files>

  <action>
Create `CHANGELOG.md` from scratch following Keep a Changelog 1.1.0 format.

Structure:
```
# Changelog

All notable changes to playwright-healer will be documented in this file.

The format is based on [Keep a Changelog](https://keepachangelog.com/en/1.1.0/),
and this project adheres to [Semantic Versioning](https://semver.org/spec/v2.0.0.html).

## [Unreleased]

## [0.1.0] - YYYY-MM-DD

First public release of playwright-healer.

### Added

[content lifted from RESEARCH.md §R-06 draft — see action below]

### Deferred (coming in v0.1.1 or v0.2)

[deferred items from RESEARCH.md §R-06 draft]

[0.1.0]: https://github.com/Sacharified/playwright-healer/releases/tag/v0.1.0
```

**Important:** At this stage (Wave 2), the `## [0.1.0]` section is a DRAFT in the [Unreleased] section. The tag date `YYYY-MM-DD` is a placeholder. In plan 06-06 (tag day), the executor will move all [Unreleased] entries to a proper [0.1.0] section and set the actual date.

Wait — per PATTERNS.md §CHANGELOG.md: "CHANGELOG: `[Unreleased]` populated in Wave 2 only; `[Unreleased]→[0.1.0]` move is Wave 4 (tag-day) task."

So the correct Wave 2 structure is:

```markdown
# Changelog

All notable changes to playwright-healer will be documented in this file.

The format is based on [Keep a Changelog](https://keepachangelog.com/en/1.1.0/),
and this project adheres to [Semantic Versioning](https://semver.org/spec/v2.0.0.html).

## [Unreleased]

### Added

[all Added entries from R-06 draft]

### Deferred (coming in v0.1.1 or v0.2)

[deferred entries from R-06 draft]

[Unreleased]: https://github.com/Sacharified/playwright-healer/compare/HEAD...HEAD
```

Lift the Added and Deferred sections from RESEARCH.md §R-06 verbatim. The content is pre-written there — use it directly with light prose editing for clarity. The `[0.1.0]` comparison URL and section move happen in plan 06-06.

Do NOT add a `[0.1.0]` section yet — it's created in plan 06-06 when the actual tag SHA is known.
  </action>

  <verification>
    <automated>test -f CHANGELOG.md && grep -c "\[Unreleased\]" CHANGELOG.md && grep -c "Keep a Changelog" CHANGELOG.md && echo "CHANGELOG_OK"</automated>
    <human-verify>
      1. Verify CHANGELOG.md starts with `# Changelog` (not `# Change Log` — keepachangelog.com 1.1.0 canonical uses "Changelog").
      2. Check that all section headers use Keep a Changelog conventions (Added, Changed, Deprecated, Removed, Fixed, Security) — NOT "Features" or "Bug Fixes" (those are Conventional Commits format).
      3. Verify the [Unreleased] section has the complete list of Added items from R-06 including: two-workflow hybrid, detection, heal loop, diff-lint, post-fix validation, PR writer, issue fallback, auto-merge gate, multi-provider support, security scaffold, composite packaging.
      4. Verify the Deferred section clearly calls out live SC#2 demo, T-05-06, app-code fix, trace-aware confidence bands.
    </human-verify>
  </verification>

  <done>
    - CHANGELOG.md exists with [Unreleased] section populated
    - `grep -c "[Unreleased]" CHANGELOG.md` returns at least 1
    - `grep -c "Keep a Changelog" CHANGELOG.md` returns 1
    - No [0.1.0] section yet (that's plan 06-06)
    - All items from RESEARCH.md §R-06 draft present under [Unreleased]
    - Section headers use Keep a Changelog format (Added, not Features)
  </done>
</task>

<task type="auto">
  <name>Task 3: Update security-lint.yml exclusion list for new docs files</name>

  <read_first>
    - 06-PATTERNS.md §security-lint.yml exclusion list update
    - .github/workflows/security-lint.yml — read the full file to find the current exclude list (lines ~29–36)
  </read_first>

  <files>.github/workflows/security-lint.yml</files>

  <action>
Read `.github/workflows/security-lint.yml` in full. Find the section that runs the `pull_request_target` grep (Check 1). It should have a list of `':(exclude)...'` pathspecs.

The current exclusion list needs additions for the new files created in this phase that legitimately contain the string "pull_request_target" in prose or comments:
- `SECURITY.md` (the security posture section explains why pull_request_target is never used)
- `CONTRIBUTING.md` (may reference CI gate checks)
- `docs/` directory (docs/auto-merge.md and docs/release-process.md may reference trigger types in explanatory prose)

Add these exclusions to the Check 1 grep command:
```
':(exclude)SECURITY.md'
':(exclude)CONTRIBUTING.md'
':(exclude)docs/'
```

Add them alongside the existing excludes. Read the file first to see the exact git grep command structure and add the new pathspecs in the same style.

After editing, verify security-lint still passes locally:
```bash
# Simulate Check 1 locally (adjust excludes list to match the updated file)
git grep -l "pull_request_target" -- \
  ':!.planning/' \
  ':!node_modules/' \
  ':!SECURITY.md' \
  ':!CONTRIBUTING.md' \
  ':!docs/'
```

Expected output should NOT include SECURITY.md, CONTRIBUTING.md, or docs/ files — those are now excluded. If any workflow YAML files appear, that is a real failure (they should not reference pull_request_target).
  </action>

  <verification>
    <automated>git grep -l "pull_request_target" -- ':!.planning/' ':!node_modules/' ':!SECURITY.md' ':!CONTRIBUTING.md' ':!docs/' | grep -v "security-lint.yml" | wc -l | xargs -I{} test {} -eq 0 && echo "LINT_CLEAN"</automated>
    <human-verify>
      1. Read the updated security-lint.yml — verify the three new exclusions (SECURITY.md, CONTRIBUTING.md, docs/) are present in the Check 1 grep command.
      2. Verify security-lint.yml itself is not broken: the workflow YAML should still be valid (check indentation, no duplicate keys).
      3. Confirm the exclusions are added to the CHECK 1 block only (the pull_request_target check), not to other checks.
    </human-verify>
  </verification>

  <done>
    - security-lint.yml Check 1 exclusion list includes SECURITY.md, CONTRIBUTING.md, docs/
    - `git grep -l "pull_request_target" -- ':!.planning/' ':!node_modules/' ':!SECURITY.md' ':!CONTRIBUTING.md' ':!docs/' | grep -v "security-lint.yml"` returns empty (or only security-lint.yml itself which is expected)
    - security-lint.yml YAML is still valid
  </done>
</task>

</tasks>

<verification>
Phase-level gates for this plan:

```bash
# Gate 1: README structure
grep -c "sequenceDiagram" README.md
grep -c "Auto-merge prerequisites" README.md
grep -c "docs/examples/gemini.yml" README.md
wc -l README.md  # should be 200+ lines

# Gate 2: CHANGELOG structure
grep -c "\[Unreleased\]" CHANGELOG.md
grep -c "Keep a Changelog" CHANGELOG.md
grep -c "### Added" CHANGELOG.md

# Gate 3: security-lint clean
git grep -l "pull_request_target" -- \
  ':!.planning/' ':!node_modules/' ':!SECURITY.md' ':!CONTRIBUTING.md' ':!docs/' \
  | grep -v "security-lint.yml"
# Expected: empty

# Gate 4: no phase numbers or GSD terminology in README
grep -n "Phase [0-9]\|GSD\|\.planning\/" README.md
# Expected: empty (consumer-facing file)
```
</verification>

<success_criteria>
- README.md is the full 11-section adoption surface (D-05): What it is, Quick start, Architecture (with Mermaid diagram), Prerequisites, Token scopes, Example workflows, Switching providers, Auto-merge, Troubleshooting, Roadmap, Contributing & Security
- README §Auto-merge prerequisites anchor preserved + links to docs/auto-merge.md
- CHANGELOG.md has [Unreleased] section with complete v0.1.0 draft entries (Added + Deferred subsections)
- CHANGELOG.md follows Keep a Changelog format (Added/Changed/Fixed headers, not Features/Bug Fixes)
- security-lint.yml updated with SECURITY.md, CONTRIBUTING.md, docs/ exclusions
- No [0.1.0] section in CHANGELOG yet (that's Wave 4 / plan 06-06)
</success_criteria>

<key_files>
  <link from="README.md ##quick-start" to="docs/examples/gemini.yml" via="Quick Start step 1 links to the gemini.yml example (created in plan 06-04)" verified="false" />
  <link from="README.md ##auto-merge-prerequisites" to="docs/auto-merge.md" via="Anchor stub + redirect sentence preserves pr-writer.ts core.warning link target" verified="false" />
  <link from="CHANGELOG.md [Unreleased]" to="plan 06-06 tag-day task" via="[Unreleased] entries moved to [0.1.0] section with date at tag time" verified="false" />
  <link from=".github/workflows/security-lint.yml" to="SECURITY.md, CONTRIBUTING.md, docs/" via="Exclusion list update prevents false positives on legitimate security prose" verified="false" />
</key_files>

<output>
After completion, create `.planning/phases/06-documentation-release/06-03-SUMMARY.md` with:
- README section count and line count
- CHANGELOG [Unreleased] item count
- security-lint.yml exclusions added
- Any prose decisions made (tone, wording choices)
- Items deferred to docs/ (plan 06-04)
</output>
