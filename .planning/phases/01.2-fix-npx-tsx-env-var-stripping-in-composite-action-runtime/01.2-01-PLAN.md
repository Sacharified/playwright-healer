---
phase: 01.2-fix-npx-tsx-env-var-stripping-in-composite-action-runtime
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - action.yml
  - .github/workflows/phase1-self-test.yml
autonomous: true
requirements:
  - G-01-PHASE-01.2
# Originating REQ-IDs from Phase 01 that this gap-closure plan re-satisfies in real CI:
#   PKG-01 (composite action functions on ubuntu-latest)
#   PKG-02 (npm ci --production install path produces a working runtime)
#   CFG-02 (every kebab-cased input reaches @actions/core.getInput intact)

must_haves:
  truths:
    - "Locally, INPUT_FOO-BAR=hello ./node_modules/.bin/tsx -e \"process.exit(process.env['INPUT_FOO-BAR']==='hello'?0:1)\" exits 0 — proving the chosen spawn shape preserves hyphenated env vars."
    - "action.yml Step 6 (line 204 region) invokes the runtime via ./node_modules/.bin/tsx src/index.ts (no npx)."
    - "action.yml Step 5 (line 169 region) invokes the readiness probe via ${{ github.action_path }}/node_modules/.bin/tsx ${{ github.action_path }}/src/healer/wait-for-ready.ts (no npx)."
    - "phase1-self-test.yml contains a dedicated top-level job named self-test-hyphenated-input-env whose only purpose is to assert a hyphenated INPUT_* env var survives the action's chosen spawn shape."
    - "Searching action.yml for 'npx tsx' returns zero matches after the fix."
    - "A live run of .github/workflows/phase1-self-test.yml on a real GitHub Actions runner (ubuntu-latest, Node 24) passes all six jobs (the five existing scenarios plus the new self-test-hyphenated-input-env job)."
  artifacts:
    - path: "action.yml"
      provides: "Composite action manifest with path-resolved tsx invocations on Steps 5 and 6"
      contains: "./node_modules/.bin/tsx src/index.ts"
    - path: "action.yml"
      provides: "Composite action manifest with path-resolved tsx invocation for wait-for-ready in Step 5"
      contains: "${{ github.action_path }}/node_modules/.bin/tsx ${{ github.action_path }}/src/healer/wait-for-ready.ts"
    - path: ".github/workflows/phase1-self-test.yml"
      provides: "Dedicated regression-test job asserting hyphenated INPUT_* env var preservation"
      contains: "self-test-hyphenated-input-env"
  key_links:
    - from: "action.yml Step 6 spawn line"
      to: "@actions/core.getInput consumers in src/*"
      via: "INPUT_* env var transit through Node spawn"
      pattern: "\\./node_modules/\\.bin/tsx src/index\\.ts"
    - from: "phase1-self-test.yml self-test-hyphenated-input-env job"
      to: "the same spawn shape used in action.yml"
      via: "shared assertion: hyphenated INPUT_FOO-BAR survives ./node_modules/.bin/tsx invocation"
      pattern: "INPUT_FOO-BAR"
---

<objective>
Fix the pre-existing Phase 01 runtime bug where `npx tsx` strips environment variables whose names contain hyphens, breaking every `core.getInput()` call for kebab-cased inputs (e.g., `INPUT_HEALER-TOKEN`, `INPUT_API-KEY`). This bug has been failing `phase1-self-test.yml` on every push since 2026-04-25 and has blocked Phase 03 SC-1/SC-3 live verification (recorded in `03-HUMAN-UAT.md` G-01).

Purpose: Restore real-CI viability of the composite action so heal/dry-run/ingest invocations actually receive their inputs. Unblocks Phase 03 SC-1/SC-3 verification.

Output: action.yml with `npx tsx` replaced by the path-resolved binary at both call sites; phase1-self-test.yml with a dedicated regression-test job asserting hyphenated `INPUT_*` env-var preservation.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01.2-fix-npx-tsx-env-var-stripping-in-composite-action-runtime/01.2-CONTEXT.md
@.planning/phases/03-manual-healer-selectors-waits-issue-fallback/03-HUMAN-UAT.md

@action.yml
@.github/workflows/phase1-self-test.yml

<interfaces>
<!-- Spawn-shape contract for the two action.yml call sites. The executor copies these strings verbatim. -->
<!-- DO NOT paraphrase. DO NOT use one shape in both places. The path resolution differs intentionally. -->

Step 6 (Run playwright-healer, has working-directory: ${{ github.action_path }}):
  CURRENT (line ~204):  npx tsx src/index.ts
  REPLACE WITH:         ./node_modules/.bin/tsx src/index.ts

Step 5 (Spawn start-command + wait for ready, NO working-directory — runs from github.workspace = consumer repo):
  CURRENT (line ~169):  npx tsx ${{ github.action_path }}/src/healer/wait-for-ready.ts
  REPLACE WITH:         ${{ github.action_path }}/node_modules/.bin/tsx ${{ github.action_path }}/src/healer/wait-for-ready.ts

Empirical-validation gate command (D-01.2-01, run from repo root with tsx@^4.21.0 already installed):
  INPUT_FOO-BAR=hello ./node_modules/.bin/tsx -e "process.exit(process.env['INPUT_FOO-BAR']==='hello'?0:1)"
  Required outcome: exit code 0.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1 [BLOCKING GATE]: Empirically validate ./node_modules/.bin/tsx preserves hyphenated env vars</name>

  <read_first>
    - .planning/phases/01.2-fix-npx-tsx-env-var-stripping-in-composite-action-runtime/01.2-CONTEXT.md (D-01.2-01 in full — this gate is locked, not optional)
    - .planning/phases/03-manual-healer-selectors-waits-issue-fallback/03-HUMAN-UAT.md (G-01 — the diagnostic this gate confirms)
    - package.json (verify tsx is in dependencies and locked at ^4.21.0)
  </read_first>

  <files>none (validation only — no file edits in this task)</files>

  <action>
This task is the empirical gate that gates Tasks 2 and 3. Do NOT proceed past this task on a non-zero exit code.

Step 1 — Ensure dependencies are installed at the same version action.yml's `npm ci --production` would install:

```bash
cd /Users/sacha/dev/playwright-healer
ls -la node_modules/.bin/tsx
```

Expect a symlink to `../tsx/dist/cli.mjs`. If absent, run `npm ci` (NOT `npm ci --production` locally — devDeps may be needed for re-running unit tests; tsx is a runtime dep so either works).

Step 2 — Run the empirical-validation command (verbatim, single line):

```bash
INPUT_FOO-BAR=hello ./node_modules/.bin/tsx -e "process.exit(process.env['INPUT_FOO-BAR']==='hello'?0:1)"
echo "exit_code=$?"
```

Step 3 — Capture the output. The PASS condition is exit code 0 (printed as `exit_code=0`).

Step 4 — Branch on result:
  - `exit_code=0` → fix is empirically valid; proceed to Task 2 and Task 3.
  - `exit_code=1` (or any non-zero) → fix is NOT valid. STOP. Do NOT edit action.yml. Surface the failure to the user with the captured output and ask whether to escalate to candidate fix #2 (compile to JS — Phase 6 release shape) or #3 (env-var bridge in bash preamble). Do NOT silently swap the fix shape; the deferred candidates have larger blast radii and CONTEXT.md D-01.2-01 explicitly requires the user to be told.

Step 5 — Record the exact command run and the exit code in your task summary so the SUMMARY.md captures the empirical evidence (per the CONTEXT.md "Specific Ideas" requirement to capture exit code as plan-summary evidence).

DO NOT use `npx tsx -e ...` for the gate — that is the broken shape, not the candidate fix. The gate must run the exact replacement string. Use the bash construct above as-is. Note: the literal env var name `INPUT_FOO-BAR` (with the hyphen) is the diagnostic — bash preserves hyphenated env-var assignments at the command line even though it cannot reference them via `$INPUT_FOO-BAR`; the Node `process.env` lookup is the test.
  </action>

  <verify>
    <automated>cd /Users/sacha/dev/playwright-healer &amp;&amp; INPUT_FOO-BAR=hello ./node_modules/.bin/tsx -e "process.exit(process.env['INPUT_FOO-BAR']==='hello'?0:1)"</automated>
  </verify>

  <acceptance_criteria>
    - Command `INPUT_FOO-BAR=hello ./node_modules/.bin/tsx -e "process.exit(process.env['INPUT_FOO-BAR']==='hello'?0:1)"` exits 0 (i.e., `echo $?` prints `0`).
    - The exact command string and the exit code are recorded in the task's contribution to SUMMARY.md.
    - If exit code is non-zero: Task 2 and Task 3 MUST NOT be started; the executor pauses for user direction (do not silently change the fix shape).
  </acceptance_criteria>

  <done>
The chosen spawn shape `./node_modules/.bin/tsx` is empirically proven to preserve hyphenated `INPUT_*` env vars on this machine. The exit-0 result is captured in the task summary as evidence. The gate is GREEN; Tasks 2 and 3 may proceed.
  </done>
</task>

<task type="auto">
  <name>Task 2: Replace npx tsx with the path-resolved binary at both action.yml call sites</name>

  <read_first>
    - action.yml (read in full — Step 5 starts at line ~152, Step 6 at line ~171; the two `npx tsx` lines are at 169 and 204)
    - .planning/phases/01.2-fix-npx-tsx-env-var-stripping-in-composite-action-runtime/01.2-CONTEXT.md (D-01.2-02 — the two call sites resolve differently; D-01.2-03 — Step 5 is in scope for consistency, not bug-fix)
    - .planning/phases/01-security-scaffold-composite-packaging/01-04-SUMMARY.md (composite manifest implementation; explains why Step 6 uses `working-directory: ${{ github.action_path }}` — preserve this invariant)
  </read_first>

  <files>action.yml</files>

  <action>
Make exactly two edits to `action.yml`. Use the Edit tool, not heredoc/sed.

Edit 1 — Step 5 (line ~169, inside the `Spawn start-command + wait for ready (heal mode)` step's `run:` block).

  FIND (exact line, no surrounding lines):
    npx tsx ${{ github.action_path }}/src/healer/wait-for-ready.ts

  REPLACE WITH (exact line):
    ${{ github.action_path }}/node_modules/.bin/tsx ${{ github.action_path }}/src/healer/wait-for-ready.ts

  Rationale (do NOT add as a comment in YAML — keep this in the plan SUMMARY only): Step 5 has no `working-directory:` set, so the composite-step default of `github.workspace` (the consumer repo) applies. A relative `./node_modules/.bin/tsx` would resolve against the consumer's tree, not the action's. Use `${{ github.action_path }}/node_modules/.bin/tsx` to anchor to the action's installed deps. Step 5's env block is currently underscored only (BASE_URL, STARTUP_TIMEOUT_SECONDS, HEALER_TOKEN, GH_OWNER, GH_REPO, TEST_TITLE, TRIGGERING_RUN_URL) — this fix is preventive, not corrective (per D-01.2-03).

Edit 2 — Step 6 (line ~204, inside the `Run playwright-healer` step's `run:` field; this step has `working-directory: ${{ github.action_path }}`).

  FIND (exact line):
    run: npx tsx src/index.ts

  REPLACE WITH (exact line):
    run: ./node_modules/.bin/tsx src/index.ts

  Rationale: Step 6 sets `working-directory: ${{ github.action_path }}`, so the relative `./node_modules/.bin/tsx` correctly resolves to the action's installed `tsx` binary (per the symlink at `node_modules/.bin/tsx -> ../tsx/dist/cli.mjs`).

DO NOT touch any other line in action.yml. DO NOT touch:
  - The `npx tsx --eval` line in `.github/workflows/security-lint.yml` (out of scope per D-01.2-02; that site does not consume INPUT_* env vars).
  - Any file under `src/` (out of scope per D-01.2-06).
  - The hyphenated `INPUT_*` env-var names in Step 6's env block (preserved per Phase 01 STATE: "@actions/core v3 getInput maps hyphens to hyphens in env var name").

After both edits, run a sanity sweep on action.yml:

```bash
grep -n "npx tsx" action.yml || echo "OK: no npx tsx remaining"
grep -nE "node_modules/\.bin/tsx" action.yml
```

Expected output: `OK: no npx tsx remaining` followed by exactly two grep lines (one for Step 5, one for Step 6).
  </action>

  <verify>
    <automated>cd /Users/sacha/dev/playwright-healer &amp;&amp; ! grep -q "npx tsx" action.yml &amp;&amp; [ "$(grep -cE 'node_modules/\.bin/tsx' action.yml)" = "2" ] &amp;&amp; grep -qE '^\s*run: \./node_modules/\.bin/tsx src/index\.ts$' action.yml &amp;&amp; grep -qE '\$\{\{ github\.action_path \}\}/node_modules/\.bin/tsx \$\{\{ github\.action_path \}\}/src/healer/wait-for-ready\.ts' action.yml</automated>
  </verify>

  <acceptance_criteria>
    - `grep -n "npx tsx" action.yml` returns no matches (exit code 1).
    - `grep -cE 'node_modules/\.bin/tsx' action.yml` returns exactly `2`.
    - `grep -E '^\s*run: \./node_modules/\.bin/tsx src/index\.ts$' action.yml` matches exactly one line in Step 6's body.
    - `grep -E '\$\{\{ github\.action_path \}\}/node_modules/\.bin/tsx \$\{\{ github\.action_path \}\}/src/healer/wait-for-ready\.ts' action.yml` matches exactly one line in Step 5's body.
    - No `src/` file is modified (`git diff --name-only -- src/` is empty).
    - `.github/workflows/security-lint.yml` is unmodified (`git diff --name-only -- .github/workflows/security-lint.yml` is empty).
    - The `INPUT_*` env block in Step 6 still uses hyphenated names (`grep -c "INPUT_.*-.*:" action.yml` returns at least `20` — the count of hyphenated INPUT keys in that block; do not refactor them).
  </acceptance_criteria>

  <done>
action.yml's two runtime spawn sites use the path-resolved tsx binary. The shape matches the empirically-validated form from Task 1. No other files are touched.
  </done>
</task>

<task type="auto">
  <name>Task 3: Add a dedicated regression-test job to phase1-self-test.yml</name>

  <read_first>
    - .github/workflows/phase1-self-test.yml (read in full — note the existing job naming convention `self-test-*` and the use of `actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd` v6.0.2 with `persist-credentials: false`)
    - .planning/phases/01.2-fix-npx-tsx-env-var-stripping-in-composite-action-runtime/01.2-CONTEXT.md (D-01.2-04 — dedicated isolated job, not piggybacked; "Specific Ideas" — minimal job that runs `npm ci --production` + the one-liner is sufficient)
    - action.yml (verify the fix from Task 2 is in place before this regression test gets added — the test would otherwise fail meaninglessly)
  </read_first>

  <files>.github/workflows/phase1-self-test.yml</files>

  <action>
Append a new top-level job to `.github/workflows/phase1-self-test.yml`. Place it as the LAST job in the `jobs:` map so the existing job order (Scenarios 1 → verify-log-mask → 2 → 3 → 4 → 5) is preserved.

Use the Edit tool to add the new job at the end of the file. The job (insert exactly this YAML block, indented at the same level as the other jobs — two spaces for the job key):

```yaml
  # ──────────────────────────────────────────────────────────────────
  # SCENARIO 6 (Phase 01.2 — npx-tsx env-var stripping regression test).
  # Asserts the action's chosen spawn shape (./node_modules/.bin/tsx) preserves
  # a hyphenated INPUT_* env var. Pre-existing bug: `npx tsx` strips env vars
  # whose names contain hyphens, so every kebab-cased input arrived as empty
  # at the spawned Node process. See 03-HUMAN-UAT.md G-01 + 01.2-CONTEXT.md.
  #
  # This job is intentionally minimal — it does NOT invoke `uses: ./` (which
  # would trigger real-mode validation noise). It runs `npm ci --production`
  # in the action dir and then exercises the spawn one-liner with a hyphenated
  # env var, asserting the var arrives at the Node process intact.
  # ──────────────────────────────────────────────────────────────────

  self-test-hyphenated-input-env:
    name: 'Self-test — hyphenated INPUT_* env var survives action spawn shape'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd  # v6.0.2
        with:
          persist-credentials: false

      - name: Set up Node
        uses: actions/setup-node@48b55a011bda9f5d6aeb4c2d9c7362e8dae4041e  # v6.4.0
        with:
          node-version: '24'

      - name: Install action runtime deps (matches action.yml Step 2)
        shell: bash
        run: npm ci --production

      - name: Assert hyphenated INPUT_* survives ./node_modules/.bin/tsx spawn
        shell: bash
        env:
          INPUT_FOO-BAR: hello
        run: |
          set -euo pipefail
          # Use the same spawn shape that action.yml Step 6 now uses.
          # If this fails, the npx-tsx fix has regressed.
          ./node_modules/.bin/tsx -e "if (process.env['INPUT_FOO-BAR'] !== 'hello') { console.error('::error::INPUT_FOO-BAR was stripped or mutated. Got:', JSON.stringify(process.env['INPUT_FOO-BAR'])); process.exit(1); } console.log('OK: INPUT_FOO-BAR survived spawn (length=' + process.env['INPUT_FOO-BAR'].length + ')');"
```

Notes for the executor:
- The SHA-pinned actions/checkout (`de0fac2e4500dabe0009e67214ff5f5447ce83dd` v6.0.2) MUST match the existing pin used elsewhere in this file (lines 25, 78, 113, 148, 205).
- The actions/setup-node pin (`48b55a011bda9f5d6aeb4c2d9c7362e8dae4041e` v6.4.0) MUST match the action.yml Step 3 pin verbatim — STATE.md records this SHA was re-verified at execution and matches RESEARCH.md exactly.
- The job key `self-test-hyphenated-input-env` follows the existing kebab-case naming convention (compare: `self-test-masking`, `self-test-invalid-mode`, `self-test-missing-api-key`, `self-test-dry-run`, `self-test-ollama-empty-key`).
- The `name:` field uses the existing pattern `'Self-test — <description>'` (em-dash U+2014, single-quoted to avoid YAML parsing quirks with the asterisk and special chars).
- The env var key `INPUT_FOO-BAR` matches the gate from Task 1 verbatim — gate and regression test fail/pass for the exact same reason (per D-01.2-04 final paragraph).
- This job intentionally does NOT use `uses: ./` — per CONTEXT.md "Specific Ideas", invoking the action would trigger Zod superRefine + dispatcher noise unrelated to the env-var-survival assertion.
- `npm ci --production` mirrors action.yml Step 2 verbatim, so the regression test exercises the same install path the action uses at runtime.

DO NOT modify any of the existing five scenarios (`self-test-masking`, `verify-log-mask`, `self-test-invalid-mode`, `self-test-missing-api-key`, `self-test-dry-run`, `self-test-ollama-empty-key`). DO NOT remove the trailing newline at end-of-file.
  </action>

  <verify>
    <automated>cd /Users/sacha/dev/playwright-healer &amp;&amp; grep -q "^  self-test-hyphenated-input-env:" .github/workflows/phase1-self-test.yml &amp;&amp; grep -qF "Self-test — hyphenated INPUT_* env var survives action spawn shape" .github/workflows/phase1-self-test.yml &amp;&amp; grep -qF "INPUT_FOO-BAR: hello" .github/workflows/phase1-self-test.yml &amp;&amp; grep -qE "\\./node_modules/\\.bin/tsx -e" .github/workflows/phase1-self-test.yml</automated>
  </verify>

  <acceptance_criteria>
    - `grep -c '^  self-test-' .github/workflows/phase1-self-test.yml` returns `6` (was 5: masking, invalid-mode, missing-api-key, dry-run, ollama-empty-key — plus new hyphenated-input-env). NOTE: `verify-log-mask` does NOT start with `self-test-`, so it is not in this count.
    - `grep -q '^  self-test-hyphenated-input-env:' .github/workflows/phase1-self-test.yml` succeeds.
    - The new job's `name:` value contains the substring `hyphenated INPUT_* env var survives action spawn shape`.
    - The new job has `env:` with `INPUT_FOO-BAR: hello`.
    - The new job's assertion step uses the literal command `./node_modules/.bin/tsx -e` (matches the spawn shape committed in Task 2).
    - The new job uses the same `actions/checkout` SHA `de0fac2e4500dabe0009e67214ff5f5447ce83dd` that the existing five jobs use.
    - The new job uses `actions/setup-node` SHA `48b55a011bda9f5d6aeb4c2d9c7362e8dae4041e` (matches action.yml Step 3).
    - All six existing scenario jobs remain present and unchanged: `git diff .github/workflows/phase1-self-test.yml` shows only ADDITIONS, zero lines removed from any pre-existing job. (Run `git diff --stat .github/workflows/phase1-self-test.yml` and confirm the diff is purely additive.)
    - The action does NOT invoke `uses: ./` in the new job (it's a minimal direct spawn test, not an action invocation). `awk '/^  self-test-hyphenated-input-env:/,/^  [a-z]/' .github/workflows/phase1-self-test.yml | grep -c "uses: \\./"` returns `0`.
  </acceptance_criteria>

  <done>
phase1-self-test.yml now has a sixth top-level job, `self-test-hyphenated-input-env`, isolated from the existing five scenarios. The job's assertion uses the same spawn shape committed in Task 2, with the same `INPUT_FOO-BAR=hello` env-var probe used in the Task 1 gate. The existing five scenarios are unmodified.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Composite action manifest → spawned tsx process | `./node_modules/.bin/tsx` invokes the lockfile-pinned binary; same trust source as the previous `npx tsx` form |
| GitHub Actions runner → tsx symlink | `node_modules/.bin/tsx → ../tsx/dist/cli.mjs` is created by `npm ci --production` from the project's lockfile; trust is inherited from `package-lock.json` integrity |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01.2-01 | T (Tampering) | Local `node_modules/.bin/tsx` symlink resolution | accept | The symlink target is created by `npm ci --production` from `package-lock.json` (lockfile-pinned `tsx@^4.21.0`). Same trust source as the previous `npx tsx` form, which also resolved to `node_modules/.bin/tsx` after a freshness check. No new attack surface. |
| T-01.2-02 | I (Information disclosure) | Self-test job env block `INPUT_FOO-BAR: hello` | accept | Literal canary value `hello` carries no secret material. Cannot leak credentials. The job runs on `ubuntu-latest` with `persist-credentials: false` on the checkout, matching the security baseline of all other scenarios. |
| T-01.2-03 | E (Elevation of privilege) | Replacing `npx` with direct binary path | accept | `npx tsx` ultimately spawned the same binary (`node_modules/.bin/tsx`) after package-resolution. The path-resolved form removes one indirection layer (the npx wrapper) but does not introduce any new privileged code path. The runtime behavior of the inner Node process is unchanged. |
| T-01.2-04 | D (Denial of service) | New self-test job `npm ci --production` step | accept | Identical to `action.yml` Step 2; bounded by GitHub's standard runner timeout. Adds ~20–40s to the workflow's longest critical path (parallel with other jobs). |

**Severity:** low. No new threat vectors introduced; the change reduces attack surface (one fewer process in the spawn chain) and adds a regression-test job whose env block is a literal non-secret canary. **Block:** none.
</threat_model>

<verification>
After all three tasks complete locally and are committed/pushed:

1. **Local verification (post-Task-2, pre-push):** Re-run the gate command from Task 1 to confirm the fix is still operative.
   ```bash
   INPUT_FOO-BAR=hello ./node_modules/.bin/tsx -e "process.exit(process.env['INPUT_FOO-BAR']==='hello'?0:1)"
   echo "exit_code=$?"  # must be 0
   ```

2. **Lint + typecheck:** Confirm no incidental damage.
   ```bash
   npm run typecheck                                        # tsc passes
   npm test                                                 # vitest passes (these mock @actions/core; will not catch the underlying bug, expected)
   ```

3. **Live CI verification (mandatory — D-01.2-05):** Push a commit that touches both files. Observe `.github/workflows/phase1-self-test.yml` runs on the resulting push event. ALL SIX jobs MUST be green:
   - `self-test-masking` (Scenario 1) → green
   - `verify-log-mask` (Scenario 1 partner) → green
   - `self-test-invalid-mode` (Scenario 2) → green
   - `self-test-missing-api-key` (Scenario 3) → green
   - `self-test-dry-run` (Scenario 4) → green
   - `self-test-ollama-empty-key` (Scenario 5) → green
   - `self-test-hyphenated-input-env` (NEW Scenario 6) → green

   If any pre-existing scenario flips from passing-pre-fix to failing-post-fix, the fix has regressed unrelated behavior — investigate before declaring done.

   If the new `self-test-hyphenated-input-env` fails on `ubuntu-latest` while the local gate passed on darwin, escalate: the spawn behavior may be platform-specific. Do NOT silently swap to candidate fix #2/#3 without user direction.

4. **Phase 03 unblock signal:** Once verification passes, update `.planning/phases/03-manual-healer-selectors-waits-issue-fallback/03-HUMAN-UAT.md` G-01 to mark the gap closed, and STATE.md "Blockers/Concerns" to remove the npx-tsx blocker line. (These are SUMMARY-time updates, not part of this plan's tasks.)
</verification>

<success_criteria>
- [ ] Empirical-validation gate command exits 0 locally before any action.yml edit (Task 1).
- [ ] `grep -n "npx tsx" action.yml` returns no matches (Task 2).
- [ ] action.yml line ~169 contains the absolute-path form: `${{ github.action_path }}/node_modules/.bin/tsx ${{ github.action_path }}/src/healer/wait-for-ready.ts` (Task 2).
- [ ] action.yml line ~204 contains the relative-path form: `./node_modules/.bin/tsx src/index.ts` (Task 2).
- [ ] No file under `src/` is modified.
- [ ] `.github/workflows/security-lint.yml` is not modified.
- [ ] `.github/workflows/phase1-self-test.yml` has a new top-level job `self-test-hyphenated-input-env` with the assertion described in Task 3 (Task 3).
- [ ] All five pre-existing self-test scenarios remain unchanged (line-level diff is purely additive).
- [ ] Live `phase1-self-test.yml` run on a real GitHub Actions ubuntu-latest runner passes all six jobs after the fix lands (verification step 3).
- [ ] Phase 03 SC-1/SC-3 verification unblocked (Phase 03 HUMAN-UAT G-01 can be closed in a follow-up STATE update).
</success_criteria>

<output>
After completion, create `.planning/phases/01.2-fix-npx-tsx-env-var-stripping-in-composite-action-runtime/01.2-01-SUMMARY.md` with:
- Empirical-validation gate output (exact command + exit code captured in Task 1)
- The two action.yml edits applied (file + line + before/after strings)
- The new phase1-self-test.yml job YAML block
- Live `phase1-self-test.yml` workflow run URL with all six jobs green (or, if local verification ran before push, a placeholder note that the live run must be observed and the URL added before the SUMMARY is committed)
- Confirmation that no files outside the two declared `files_modified` entries were touched
</output>
