---
status: passed
phase: 01.2-fix-npx-tsx-env-var-stripping-in-composite-action-runtime
source: [01.2-VERIFICATION.md]
started: 2026-04-27T20:15:00Z
updated: 2026-04-27T20:35:00Z
forward_handoff: 01.3-fix-pre-existing-phase1-self-test-design-bugs
---

## Current Test

[live ubuntu-latest verification complete — Phase 01.2 goal met]

## Tests

### 1. Live phase1-self-test.yml run on ubuntu-latest (D-01.2-05)
expected: All six jobs of `.github/workflows/phase1-self-test.yml` pass on ubuntu-latest + Node 24 on the next push event. The new `self-test-hyphenated-input-env` job prints `OK: INPUT_FOO-BAR survived spawn (length=5)`. The five pre-existing scenarios (`self-test-masking`, `verify-log-mask`, `self-test-invalid-mode`, `self-test-missing-api-key`, `self-test-dry-run`, `self-test-ollama-empty-key`) remain green — no regression introduced by the `npx tsx` → `./node_modules/.bin/tsx` change. Run URL appended to `01.2-01-SUMMARY.md` "Verification Status / Live CI" section. Once green, `03-HUMAN-UAT.md` G-01 can be marked closed and the Phase 03 SC-1/SC-3 unblock-pending bullet removed from STATE.md.
result: passed (with scope clarification — see notes)

notes: |
  Live run executed 2026-04-27 ~19:12 UTC on ubuntu-24.04 + Node 24. Direct
  verification of Phase 01.2's actual goal (hyphenated INPUT_* preservation)
  passed:

  - **Scenario 6 (self-test-hyphenated-input-env) — GREEN.** Authoritative
    signal for D-01.2-05. INPUT_FOO-BAR=hello survived `./node_modules/.bin/tsx`
    spawn intact on the real runner.
  - **Scenario 1 Job A (self-test-masking) — GREEN.** Action ran end-to-end
    to completion. `core.getInput('healer-token', { required: true })` no
    longer throws — confirms env-var stripping is fixed for required inputs.
  - **Scenario 4 dispatcher output — confirmed.** `playwright-healer — dry-run
    summary` markdown table is visible in the GitHub UI Summary tab. Dispatcher
    `core.summary.write()` works correctly. (The bash assertion on
    `$GITHUB_STEP_SUMMARY` is the bug, not the dispatcher.)

  The "all six jobs green" criterion in the original expected text was
  overspecified: it bundled three pre-existing test-design bugs under a
  Phase 01.2 verification clause. The expected behavior of
  Phase 01.2's deliverable (env-var preservation) is met. The three failing
  scenarios have root causes that pre-date Phase 01.2 — they are forwarded
  to a new Phase 01.3:

    1. **Scenario 4 (self-test-dry-run)** — bash assertion treats
       `$GITHUB_STEP_SUMMARY` as job-wide, but it is per-step. Pre-existing
       since Phase 01-06.
    2. **Scenario 5 (self-test-ollama-empty-key)** — same root cause as #1,
       inherited from Scenario 4's pattern when 01.1 added the ollama scenario.
    3. **Scenario 1 Job B (verify-log-mask)** — runner emits `with: api-key:
       <canary>` (line 150) and `INPUT_API-KEY: <canary>` (line 205) in step
       headers BEFORE the action body runs `core.setSecret()`. The TWO-JOB
       pattern's premise is structurally unable to mask the literal `with:`
       value. Pre-01.2, Job A failed early at `getInput`-required, so this
       latent leak was never reached. Post-01.2, Job A runs end-to-end and
       Job B's grep now actually executes.

  Each of these is unmasked by 01.2's fix (action now runs through), not
  caused by it. They are tracked under Phase 01.3 (forward_handoff).

## Summary

total: 1
passed: 1
issues: 0
pending: 0
skipped: 0
blocked: 0

## Gaps

(none — three pre-existing test-design bugs forwarded to Phase 01.3, see notes above)
