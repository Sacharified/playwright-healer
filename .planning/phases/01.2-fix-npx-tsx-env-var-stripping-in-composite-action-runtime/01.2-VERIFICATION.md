---
phase: 01.2-fix-npx-tsx-env-var-stripping-in-composite-action-runtime
verified: 2026-04-27T20:10:00Z
status: human_needed
score: 5/6 must-haves verified (5 design-side truths verified; 1 live-CI truth pending push)
overrides_applied: 0
human_verification:
  - test: "Push the Phase 01.2 commits to GitHub and observe the resulting phase1-self-test.yml run on ubuntu-latest. All six jobs MUST be green (self-test-masking, verify-log-mask, self-test-invalid-mode, self-test-missing-api-key, self-test-dry-run, self-test-ollama-empty-key, plus NEW self-test-hyphenated-input-env)."
    expected: "All six jobs of .github/workflows/phase1-self-test.yml pass on ubuntu-latest + Node 24 on the next push event. self-test-hyphenated-input-env prints 'OK: INPUT_FOO-BAR survived spawn (length=5)'. The five pre-existing scenarios remain green (no regression introduced by the spawn-shape change). Run URL appended to 01.2-01-SUMMARY.md 'Verification Status / Live CI' section."
    why_human: "The bug `npx tsx` strips hyphenated env vars only reproduces on ubuntu-latest (per 01.2-CONTEXT.md D-01.2-05 and 03-HUMAN-UAT.md G-01). The local darwin gate confirmed that ./node_modules/.bin/tsx preserves hyphenated env vars (exit code 0), but the regression authority is the ubuntu-latest runner shell where the bug manifests. This requires a real GitHub Actions runner — not reproducible locally. The plan and SUMMARY both explicitly mark this as 'Live CI verification pending push' (D-01.2-05 mandate). Per the verifier's phase-specific guidance, this is a human verification item, not a code-level gap."
---

# Phase 01.2: Fix npx tsx Env-Var Stripping in Composite Action Runtime — Verification Report

**Phase Goal:** action.yml's runtime spawn shape preserves hyphenated `INPUT_*` env vars end-to-end so every kebab-cased input reaches `@actions/core.getInput` intact, and `phase1-self-test.yml` has a dedicated regression-test job asserting this invariant — restoring real-CI viability of the composite action and unblocking Phase 03 SC-1/SC-3 live verification (per `03-HUMAN-UAT.md` G-01).

**Verified:** 2026-04-27T20:10:00Z
**Status:** human_needed
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths

| #   | Truth                                                                                                                                                                                | Status     | Evidence                                                                                                                                                                                                                                                                                                       |
| --- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ---------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 1   | Locally, `INPUT_FOO-BAR=hello ./node_modules/.bin/tsx -e "process.exit(process.env['INPUT_FOO-BAR']==='hello'?0:1)"` exits 0                                                          | ✓ VERIFIED | Re-ran during verification at 2026-04-27T20:10:00Z (using `env "INPUT_FOO-BAR=hello" ...` form because zsh and bash 3.2 reject hyphenated assignment-prefixes — same accommodation documented in SUMMARY "Deviations" section). Output: `exit_code=0`. Gate is GREEN.                                          |
| 2   | action.yml Step 6 (line 204 region) invokes the runtime via `./node_modules/.bin/tsx src/index.ts` (no npx)                                                                            | ✓ VERIFIED | `action.yml:204` reads `run: ./node_modules/.bin/tsx src/index.ts`. Step has `working-directory: ${{ github.action_path }}` (line 174), so the relative path resolves to the action's installed deps as designed.                                                                                              |
| 3   | action.yml Step 5 (line 169 region) invokes the readiness probe via `${{ github.action_path }}/node_modules/.bin/tsx ${{ github.action_path }}/src/healer/wait-for-ready.ts` (no npx) | ✓ VERIFIED | `action.yml:169` reads `${{ github.action_path }}/node_modules/.bin/tsx ${{ github.action_path }}/src/healer/wait-for-ready.ts`. Step has no `working-directory` (intentional per D-01.2-03 — start-command must run from `github.workspace`), so absolute action_path-prefixed paths are correct.              |
| 4   | phase1-self-test.yml contains a dedicated top-level job named `self-test-hyphenated-input-env` whose only purpose is to assert a hyphenated INPUT_* env var survives the action's chosen spawn shape | ✓ VERIFIED | `.github/workflows/phase1-self-test.yml:259` declares `self-test-hyphenated-input-env:`. Job uses Node 24, `npm ci --production`, and the same `./node_modules/.bin/tsx -e` spawn shape committed in action.yml Step 6. `env: INPUT_FOO-BAR: hello` (line 279); assertion script (line 284) emits `::error::` with redacted output if env var is stripped. Job is isolated — does NOT use `uses: ./` (per D-01.2-04). |
| 5   | Searching action.yml for 'npx tsx' returns zero matches after the fix                                                                                                                  | ✓ VERIFIED | `grep -n "npx tsx" action.yml` returns no matches (exit code 1). Both former call sites (lines 169, 204) have been replaced by path-resolved tsx forms.                                                                                                                                                       |
| 6   | A live run of `.github/workflows/phase1-self-test.yml` on a real GitHub Actions runner (ubuntu-latest, Node 24) passes all six jobs                                                    | ? HUMAN    | **Pending push.** Local empirical gate confirms the fix shape on darwin/zsh + Node 24 (exit 0). The bug is platform-specific to the ubuntu-latest runner shell (per 01.2-CONTEXT.md D-01.2-05 + 03-HUMAN-UAT.md G-01); cannot be verified locally. Live run URL must be observed and appended to 01.2-01-SUMMARY.md "Verification Status / Live CI" section. See Human Verification Required below. |

**Score:** 5/6 truths verified (5 verified design-side; 1 routed to human verification per phase-specific caveat)

### Required Artifacts

| Artifact                                  | Expected                                                                                  | Status     | Details                                                                                                                                                                                                                            |
| ----------------------------------------- | ----------------------------------------------------------------------------------------- | ---------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `action.yml`                              | Composite manifest with path-resolved tsx invocations on Steps 5 and 6                    | ✓ VERIFIED | Both lines present at 169 and 204; sweep confirms `grep -cE 'node_modules/\.bin/tsx' action.yml` returns 2; `grep -n 'npx tsx'` returns 0; the 23 hyphenated `INPUT_*` env keys (line 176-201) are preserved untouched.            |
| `action.yml` (Step 5 form)                | Step 5 uses `${{ github.action_path }}/node_modules/.bin/tsx ${{ github.action_path }}/src/healer/wait-for-ready.ts` | ✓ VERIFIED | Confirmed on line 169. Absolute action_path prefix is correct because Step 5 has no `working-directory:` (defaults to `github.workspace` = consumer repo).                                                                          |
| `.github/workflows/phase1-self-test.yml`  | Dedicated regression-test job asserting hyphenated INPUT_* env var preservation           | ✓ VERIFIED | Job `self-test-hyphenated-input-env` declared at line 259; `INPUT_FOO-BAR: hello` env at 279; `./node_modules/.bin/tsx -e` assertion at 284. Diff vs prior commit is purely additive (40 insertions, 0 deletions per `git diff --stat`). |

### Key Link Verification

| From                                                       | To                                          | Via                                                                                            | Status   | Details                                                                                                                                                                                                                                                                                                                |
| ---------------------------------------------------------- | ------------------------------------------- | ---------------------------------------------------------------------------------------------- | -------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| action.yml Step 6 spawn line                                | `@actions/core.getInput` consumers in src/* | INPUT_* env var transit through Node spawn (pattern: `\./node_modules/\.bin/tsx src/index\.ts`) | ✓ WIRED  | `grep -E '^\s*run: \./node_modules/\.bin/tsx src/index\.ts$' action.yml` matches exactly one line (204) inside Step 6's body. Step 6's env block (lines 175-201) declares all hyphenated INPUT_* keys; the spawn shape is now empirically proven (Truth 1) to preserve them.                                            |
| phase1-self-test.yml `self-test-hyphenated-input-env` job   | the same spawn shape used in action.yml      | shared assertion: hyphenated INPUT_FOO-BAR survives `./node_modules/.bin/tsx` invocation        | ✓ WIRED  | Job's assertion command (line 284) literally uses `./node_modules/.bin/tsx -e "if (process.env['INPUT_FOO-BAR'] !== 'hello') { ... process.exit(1); } ..."` — the SAME spawn shape that action.yml Step 6 uses (line 204). Probe key `INPUT_FOO-BAR` matches the local gate from Task 1 verbatim. Failure => `::error::` annotation surfaces directly at the spawn line per D-01.2-04. |

### Data-Flow Trace (Level 4)

Not applicable — Phase 01.2 modifies a YAML manifest and a CI workflow, not a UI component or data-rendering artifact. The "data flow" in scope is the env var → spawned process → `process.env` lookup; that flow is verified by Truth 1 (local gate) and Truth 6 (live CI gate, pending).

### Behavioral Spot-Checks

| Behavior                                                                                                  | Command                                                                                                                              | Result      | Status |
| --------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------ | ----------- | ------ |
| `./node_modules/.bin/tsx` preserves hyphenated env var on darwin                                          | `env "INPUT_FOO-BAR=hello" ./node_modules/.bin/tsx -e "process.exit(process.env['INPUT_FOO-BAR']==='hello'?0:1)"; echo "exit_code=$?"` | `exit_code=0` | ✓ PASS |
| Project typecheck still passes                                                                            | `npm run typecheck`                                                                                                                  | tsc clean (no output) | ✓ PASS |
| Project unit tests still pass (no regression from spawn-shape change)                                     | `npm test`                                                                                                                           | 22 test files / 241 tests passed | ✓ PASS |
| `action.yml` no longer references `npx tsx`                                                               | `grep -n "npx tsx" action.yml \|\| echo OK`                                                                                          | `OK` (no matches) | ✓ PASS |
| `action.yml` references the path-resolved binary at exactly two sites                                     | `grep -cE 'node_modules/\.bin/tsx' action.yml`                                                                                       | `2` | ✓ PASS |
| phase1-self-test.yml has six top-level `self-test-*` jobs (was 5: masking, invalid-mode, missing-api-key, dry-run, ollama-empty-key — now +hyphenated-input-env) | `grep -c '^  self-test-' .github/workflows/phase1-self-test.yml` | `6` | ✓ PASS |
| New regression job declares the hyphenated env probe                                                       | `grep -n "INPUT_FOO-BAR" .github/workflows/phase1-self-test.yml`                                                                     | matches at lines 279 and 284 | ✓ PASS |
| Phase commits exist (Task 2 fix, Task 3 test, plan-completion docs)                                        | `git log --oneline c0c2f21 a37df83 b523ea5`                                                                                          | All three present (latest: `565fbcb docs(01.2): add code review report`) | ✓ PASS |
| Diff is purely additive in phase1-self-test.yml (no removals from existing jobs)                          | `git diff a37df83~1 a37df83 --stat .github/workflows/phase1-self-test.yml`                                                            | `40 ++++++++++++++++++++++++++++++++++ 1 file changed, 40 insertions(+)` | ✓ PASS |
| No `src/` files modified by Phase 01.2 commits                                                            | `git diff c0c2f21~1 a37df83 --name-only -- src/`                                                                                     | empty | ✓ PASS |
| `.github/workflows/security-lint.yml` unmodified (out of scope per D-01.2-02)                             | `git diff c0c2f21~1 a37df83 --name-only -- .github/workflows/security-lint.yml`                                                       | empty | ✓ PASS |
| Behavior on ubuntu-latest                                                                                  | (n/a — requires GitHub Actions runner)                                                                                              | not runnable locally | ? SKIP — routed to human verification |

### Requirements Coverage

| Requirement      | Source Plan | Description                                                                                              | Status                | Evidence                                                                                                                                                                                                                                                                                                                                |
| ---------------- | ----------- | -------------------------------------------------------------------------------------------------------- | --------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| G-01-PHASE-01.2  | 01.2-01-PLAN | Phase 01.2 gap-closure: action.yml spawn shape preserves hyphenated INPUT_* env vars + dedicated regression job | ✓ SATISFIED (design); ? NEEDS HUMAN (real CI) | This ID is not an entry in REQUIREMENTS.md's v1 list — it is a phase-scoped gap-closure ID coined in 03-HUMAN-UAT.md G-01 and ROADMAP.md Phase 01.2. The design-side artifacts are all in place (Truths 1–5; Artifacts; Key Links; Spot-Checks). The "real CI" clause re-satisfies originating PKG-01/PKG-02/CFG-02 once the live ubuntu-latest run is observed (Truth 6, human verification). |
| PKG-01 (re-verify) | n/a (originating: 01-04) | Action ships as composite GitHub Action (`runs.using: composite`) — re-verified in real CI by Phase 01.2 | ✓ SATISFIED (design); ? NEEDS HUMAN (real CI) | `action.yml:124` `using: composite`. The path-resolved tsx fix preserves the composite-action shape; runtime install path (`npm ci --production` Step 2 → `node_modules/.bin/tsx`) is now exercised end-to-end by the new regression job. Real-CI confirmation is the live phase1-self-test.yml run.                                          |
| PKG-02 (re-verify) | n/a (originating: 01-04) | `npm ci --production` runs as the first composite step → installed runtime path produces a working tsx binary | ✓ SATISFIED (design); ? NEEDS HUMAN (real CI) | Step 2 (`action.yml:135-138`) is unchanged. The new regression job (`phase1-self-test.yml:272-274`) exercises the same `npm ci --production` install path and then invokes `./node_modules/.bin/tsx` against it — proving the install path yields a working binary capable of executing the runtime spawn.                                  |
| CFG-02 (re-verify) | n/a (originating: 01-04) | All hyphenated kebab-case inputs reach `@actions/core.getInput` intact — bug-fix scope                  | ✓ SATISFIED (design); ? NEEDS HUMAN (real CI) | Step 6's hyphenated `INPUT_*` env block (23 keys) is preserved untouched (`grep -c "INPUT_.*-.*:" action.yml` returns 23). The local gate proves `./node_modules/.bin/tsx` preserves a hyphenated env var to the Node process. Live ubuntu-latest run is the authoritative re-verification.                                                  |

**Orphan check:** REQUIREMENTS.md does not map any additional REQ-IDs to Phase 01.2 (the table at lines 178-249 only enumerates PKG/CFG/ING/STA/DET/SEC/HEA/FIX/VAL/PRI/MRG/DOC IDs, all phase-tagged via the original phases 1–6). G-01-PHASE-01.2 is a phase-scoped gap-closure ID, intentionally not registered in the v1 traceability table because it re-satisfies existing IDs rather than introducing a new capability. No orphans.

### Anti-Patterns Found

| File                                       | Line | Pattern                                                                                                  | Severity   | Impact                                                                                                                                                                                                                                                                                                                                                          |
| ------------------------------------------ | ---- | -------------------------------------------------------------------------------------------------------- | ---------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `.github/workflows/phase1-self-test.yml`   | 272-284 | Regression-test job runs `npm ci --production` and `./node_modules/.bin/tsx` in default working directory rather than `${{ github.action_path }}` | ⚠️ Warning | From 01.2-REVIEW.md WR-01: in this workflow's checkout the default workspace and `github.action_path` resolve to the same directory, so the test passes today; but a future repo refactor could decouple them and the regression test would silently exercise a different install tree than action.yml Step 6 does. NOT a goal-blocker — the test still asserts the spawn-shape invariant correctly today. Recommendation deferred to a follow-up cleanup. |
| `action.yml` (pre-existing)                | 167  | `bash -c "exec ${{ inputs.start-command }}" &` — double-interpolation of consumer-supplied input          | ℹ️ Info    | From 01.2-REVIEW.md IN-01: pre-existing shell-injection exposure on the trusted-by-convention `start-command` input (Phase 01 inheritance, not introduced by 01.2). Out of scope for this phase per D-01.2-02 and security model.                                                                                                                            |
| `action.yml`                                | 156-169 | Step 5 vs Step 6 spawn-shape asymmetry not flagged in inline comments                                    | ℹ️ Info    | From 01.2-REVIEW.md IN-02: the asymmetry is correct and intentional (D-01.2-03 + summary), but a future contributor unifying "for consistency" would break Step 5. Comment hardening recommended; not a goal-blocker.                                                                                                                                       |
| `action.yml` (pre-existing)                | 131  | `ref: ${{ inputs.commit-sha }}` unquoted                                                                  | ℹ️ Info    | From 01.2-REVIEW.md IN-03: pre-existing; safe for 40-char SHAs but YAML-unquoted-scalar footgun in general. Not introduced by 01.2.                                                                                                                                                                                                                          |
| `.github/workflows/phase1-self-test.yml`   | 284  | Missing trailing newline at EOF (Read/grep snapshot)                                                     | ℹ️ Info    | From 01.2-REVIEW.md IN-04: cosmetic; YAML parsers accept it; linters may flag. Not a goal-blocker.                                                                                                                                                                                                                                                          |

No 🛑 Blocker anti-patterns found. The single ⚠️ Warning (WR-01) is a robustness improvement, not a correctness defect — the regression test passes today and asserts the right invariant.

### Human Verification Required

#### 1. Live `phase1-self-test.yml` ubuntu-latest run (D-01.2-05 mandate)

**Test:** Push the Phase 01.2 commits to a remote branch (e.g., `main` or a feature branch) on GitHub. Observe `.github/workflows/phase1-self-test.yml` runs on the resulting push event (the workflow triggers `on: push` and `on: pull_request`).

**Expected:**
- All six top-level jobs are green:
  - `self-test-masking` (Scenario 1)
  - `verify-log-mask` (Scenario 1 partner)
  - `self-test-invalid-mode` (Scenario 2)
  - `self-test-missing-api-key` (Scenario 3)
  - `self-test-dry-run` (Scenario 4)
  - `self-test-ollama-empty-key` (Scenario 5)
  - `self-test-hyphenated-input-env` (NEW Scenario 6 — must print `OK: INPUT_FOO-BAR survived spawn (length=5)` in its log)
- Five pre-existing scenarios remain green (no regression from the spawn-shape change — all paths that were previously passing on darwin via local mocks now pass on ubuntu-latest with the runtime fix).
- The run URL is appended to `01.2-01-SUMMARY.md` "Verification Status / Live CI" section in a follow-up commit.

**Why human:** The `npx tsx` env-var-stripping bug is platform-specific to the ubuntu-latest runner shell (per 01.2-CONTEXT.md D-01.2-05 and 03-HUMAN-UAT.md G-01 — confirmed empirically: `npx tsx` ALSO preserves hyphenated env vars on darwin, so a passing local gate does not prove the fix on ubuntu). Authoritative re-verification of PKG-01/PKG-02/CFG-02 in real CI requires a GitHub Actions ubuntu-latest runner, which cannot be invoked from this verification context. The plan and SUMMARY both explicitly designate this as "Live CI verification — MANDATORY for plan to be considered fully verified" (D-01.2-05). Per the verifier's phase-specific caveat for 01.2, this is a human verification item, not a code-level gap.

**Fallback if regression:** If `self-test-hyphenated-input-env` fails on ubuntu-latest while local gate passed on darwin, escalate per CONTEXT.md D-01.2-01 — do NOT silently swap to candidate fix #2 (compile to JS) or #3 (env-var bridge). Surface the run URL + log output and request user direction.

### Gaps Summary

No code-level gaps. All five design-side observable truths are verified, all required artifacts are present and substantive, both key links are wired, all behavioral spot-checks pass, and project tests/typecheck remain clean. The two task commits (`c0c2f21` fix, `a37df83` test) are atomic and purely additive in the workflow file. SUMMARY.md self-check is PASSED.

The single non-VERIFIED item — Truth 6, the live ubuntu-latest run — is intentionally and explicitly routed to human verification per the phase plan (D-01.2-05) and SUMMARY ("Verification Status / Live CI" placeholder for the run URL). The plan is design-complete; the empirical proof on the runner where the bug manifests is pending the next push.

---

_Verified: 2026-04-27T20:10:00Z_
_Verifier: Claude (gsd-verifier)_
