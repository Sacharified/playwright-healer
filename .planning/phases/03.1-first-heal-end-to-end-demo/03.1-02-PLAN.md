---
phase: 03.1-first-heal-end-to-end-demo
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - action.yml
autonomous: true
requirements:
  - PKG-01
  - PKG-02
  - SEC-01

must_haves:
  truths:
    - "action.yml declares three new inputs: skip-deterministic-check, skip-post-fix-validation, skip-diff-lint (all default 'false')"
    - "Step 6 env block contains INPUT_SKIP-DETERMINISTIC-CHECK, INPUT_SKIP-POST-FIX-VALIDATION, INPUT_SKIP-DIFF-LINT"
    - "A git credential step runs before Step 6 (heal mode only) so git push works despite persist-credentials: false"
    - "A Playwright browser install step runs after npm ci and before Step 4 (heal mode only) using the action's own playwright binary"
    - "All existing phase1-self-test.yml dry-run scenarios still pass (no behavior regression from new steps)"
  artifacts:
    - path: "action.yml"
      provides: "Three new inputs + git cred step + browser install step + expanded env block"
      contains: "skip-deterministic-check"
  key_links:
    - from: "sc1-healer.yml (Plan 03)"
      to: "action.yml inputs.skip-deterministic-check"
      via: "with: skip-deterministic-check: 'true'"
      pattern: "skip-deterministic-check"
    - from: "action.yml git cred step"
      to: "fix-applier.ts git push"
      via: "git config url.insteadOf transparent credential substitution"
      pattern: "url\\..*insteadOf"
    - from: "action.yml playwright install step"
      to: "gemini.ts StdioClientTransport"
      via: "installs chromium into version-specific playwright cache"
      pattern: "playwright install chromium"
---

<objective>
Add infrastructure-level fixes to action.yml: git credentials for push (CRACK-2), Playwright browser binary install (CRACK-3), and three new skip-flag inputs wired to the env block.

Purpose: The action's composite steps will fail even after Plan 01's code fixes because: (1) `persist-credentials: false` strips the runner's git credential helper — `fix-applier.ts`'s `git push` gets HTTP 401; (2) `npm ci --production` installs `@playwright/mcp@0.0.70` but not its version-specific Chromium binary — MCP spawn crashes. Both are pure action.yml changes with no TypeScript touch.

Output: Updated action.yml with two new heal-mode steps and three new inputs wired into the Step 6 env block.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03.1-first-heal-end-to-end-demo/03.1-CONTEXT.md
@.planning/phases/03.1-first-heal-end-to-end-demo/03.1-RESEARCH.md

<interfaces>
<!-- Existing action.yml step ordering — executor inserts into this structure. -->

Current action.yml step sequence (relevant steps):
  Step 1: Checkout dispatch SHA (heal mode, if commit-sha != '')
  Step 2: Install action dependencies — npm ci --production
  Step 3: Set up Node (setup-node@v6.4.0)
  Step 4: Run setup-command (sync, heal mode)
  Step 5: Spawn start-command + wait for ready (heal mode)
  Step 6: Run playwright-healer (tsx src/index.ts) — env block here
  Step 7: Cleanup leaked processes (always, heal mode)

New steps to insert (correct positions):
  After Step 3 (Node set up), before Step 4 (setup-command):
    → "Install Playwright browser (for Playwright MCP)" — heal mode only
  After Step 5 (app started), before Step 6 (tsx run):
    → "Configure git credentials (heal mode)" — heal mode only

Rationale for ordering:
  - Browser install must come AFTER setup-node (needs node to run playwright CLI)
  - Browser install must come BEFORE setup-command (idempotent, but avoids race)
  - Git creds must come BEFORE Step 6 (fix-applier runs inside tsx; creds needed by Step 9)
  - Git creds AFTER app start (app start is the last user-provided command before the agent runs)

CRACK-3 fix (exact pattern from RESEARCH):
```yaml
- name: Install Playwright browser (for Playwright MCP)
  if: inputs.mode == 'heal'
  shell: bash
  working-directory: ${{ github.action_path }}
  run: node_modules/.bin/playwright install chromium --with-deps
```
WARNING: must use `node_modules/.bin/playwright`, NOT system `playwright` or `npx playwright`.
The action's own node_modules has playwright@1.60.0-alpha (matching @playwright/mcp@0.0.70).
System playwright or consumer's playwright is ^1.49.1 — installs wrong binary version.

CRACK-2 fix (exact pattern from RESEARCH):
```yaml
- name: Configure git credentials (heal mode)
  if: inputs.mode == 'heal'
  shell: bash
  env:
    HEALER_TOKEN: ${{ inputs.healer-token }}
  run: |
    git config --global url."https://x-access-token:${HEALER_TOKEN}@github.com/".insteadOf "https://github.com/"
```
This is transparent to fix-applier.ts — no TypeScript changes needed.

New inputs to add (after existing startup-timeout-seconds input):
```yaml
skip-deterministic-check:
  description: 'Skip the deterministic-failure gate (demo mode). Default: false'
  required: false
  default: 'false'
skip-post-fix-validation:
  description: 'Skip post-fix Playwright validation reruns (demo mode; CI is the truth). Default: false'
  required: false
  default: 'false'
skip-diff-lint:
  description: 'Skip diff-lint forbidden-pattern check (demo mode). Default: false'
  required: false
  default: 'false'
```

New env vars to add to Step 6 env block:
```yaml
INPUT_SKIP-DETERMINISTIC-CHECK: ${{ inputs.skip-deterministic-check }}
INPUT_SKIP-POST-FIX-VALIDATION: ${{ inputs.skip-post-fix-validation }}
INPUT_SKIP-DIFF-LINT: ${{ inputs.skip-diff-lint }}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add new inputs, insert CRACK-2 + CRACK-3 steps, wire env vars in Step 6</name>
  <files>action.yml</files>
  <action>
    Make four edits to action.yml in sequence. Read the file once, apply all changes, write once.

    **Edit 1 — New inputs** (after the `startup-timeout-seconds` input block, before `outputs:`):
    Add three new inputs:
    ```yaml
      skip-deterministic-check:
        description: 'Skip the deterministic-failure gate (demo mode only). Set true to let the healer attempt a fix on an always-failing test. Default: false'
        required: false
        default: 'false'
      skip-post-fix-validation:
        description: 'Skip post-fix Playwright validation reruns (demo mode only). fixture-ci.yml on the opened PR is the truth source. Default: false'
        required: false
        default: 'false'
      skip-diff-lint:
        description: 'Skip diff-lint forbidden-pattern check (demo mode only). Re-enable in Phase 04. Default: false'
        required: false
        default: 'false'
    ```

    **Edit 2 — CRACK-3: Playwright browser install** (insert AFTER Step 3 setup-node, BEFORE Step 4 setup-command):
    ```yaml
        # Step 3.1 (NEW Phase 03.1) — Install Playwright browser binary for @playwright/mcp (CRACK-3 fix)
        # Must use action's own node_modules/.bin/playwright (version 1.60.0-alpha matching MCP).
        # Do NOT use system playwright or npx playwright — wrong binary version would be installed.
        - name: Install Playwright browser (for Playwright MCP)
          if: inputs.mode == 'heal'
          shell: bash
          working-directory: ${{ github.action_path }}
          run: node_modules/.bin/playwright install chromium --with-deps
    ```

    **Edit 3 — CRACK-2: Git credential step** (insert AFTER Step 5 start-command, BEFORE Step 6 tsx run):
    ```yaml
        # Step 5.1 (NEW Phase 03.1) — Configure git credentials for fix-applier push (CRACK-2 fix)
        # persist-credentials: false (SEC-01) strips the runner's credential helper.
        # fix-applier.ts calls 'git push' with no explicit credentials; this git config
        # provides HTTPS credential substitution transparently — no TypeScript changes needed.
        - name: Configure git credentials (heal mode)
          if: inputs.mode == 'heal'
          shell: bash
          env:
            HEALER_TOKEN: ${{ inputs.healer-token }}
          run: |
            git config --global url."https://x-access-token:${HEALER_TOKEN}@github.com/".insteadOf "https://github.com/"
    ```

    **Edit 4 — Step 6 env block extension** (add three lines to the existing env: block in Step 6, after INPUT_STARTUP-TIMEOUT-SECONDS):
    ```yaml
            INPUT_SKIP-DETERMINISTIC-CHECK: ${{ inputs.skip-deterministic-check }}
            INPUT_SKIP-POST-FIX-VALIDATION: ${{ inputs.skip-post-fix-validation }}
            INPUT_SKIP-DIFF-LINT: ${{ inputs.skip-diff-lint }}
    ```

    Verify YAML indentation matches surrounding content. Step-level items align with other `- name:` lines. Env var lines align with other `INPUT_*:` lines.
  </action>
  <verify>
    <automated>
      # Structural checks (actionlint if available; else basic grep + tsc):
      which actionlint && actionlint action.yml || echo "actionlint not installed — skip"
      # Confirm new inputs present:
      grep -c "skip-deterministic-check\|skip-post-fix-validation\|skip-diff-lint" action.yml
      # Confirm new steps present:
      grep -c "Configure git credentials\|Install Playwright browser" action.yml
      # Confirm env vars wired:
      grep -c "INPUT_SKIP-" action.yml
      # Existing test suite still passes (no TypeScript regression from new env var names):
      npx vitest run
    </automated>
  </verify>
  <done>
    - action.yml has 3 new inputs declared under `inputs:`
    - action.yml has 2 new steps: "Install Playwright browser (for Playwright MCP)" + "Configure git credentials (heal mode)"
    - action.yml Step 6 env block has 3 new INPUT_SKIP-* lines
    - Both new steps have `if: inputs.mode == 'heal'` (no behavior change for ingest/dry-run modes)
    - grep confirms: 6 occurrences of skip-deterministic-check (input def + env var + 2 per step comment), etc.
    - npx vitest run passes (253+ tests)
    - actionlint exits 0 (if available)
  </done>
</task>

</tasks>

<verification>
Regression guard — existing 8 dry-run/ingest scenarios in phase1-self-test.yml are unaffected because:
- New steps are `if: inputs.mode == 'heal'` — they don't execute for ingest/dry-run
- New env vars default 'false' — no behavior change when not set
- New inputs are `required: false` with defaults — existing workflow callers need no changes

Confirm with:
```bash
npx vitest run    # must pass 253+ tests
grep "if: inputs.mode == 'heal'" action.yml | wc -l   # should return >= 4 (existing + 2 new)
```
</verification>

<success_criteria>
- action.yml valid YAML with 3 new inputs (default 'false')
- 2 new heal-mode-only steps inserted in correct position (browser install after setup-node, git creds before tsx run)
- Step 6 env block extended with 3 INPUT_SKIP-* lines
- All 253+ unit tests still pass
- No new behavior in ingest/dry-run modes (new steps guarded by `if: inputs.mode == 'heal'`)
</success_criteria>

<output>
After completion, create `.planning/phases/03.1-first-heal-end-to-end-demo/03.1-02-SUMMARY.md`
</output>
