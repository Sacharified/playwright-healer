---
phase: 04-auto-dispatch-full-fix-classes-deduplication
plan: 05
type: execute
wave: 5
depends_on: [01, 02, 03, 04]
files_modified:
  - .github/workflows/e2e-heal-self.yml
  - fixture/tests/broken-assertion.spec.ts
  - fixture/tests/broken-selector.spec.ts
autonomous: false
requirements: [DET-07, FIX-07]
tags: [e2e, verification, concurrency, fixture, manual-uat]
must_haves:
  truths:
    - "`.github/workflows/e2e-heal-self.yml` declares a `concurrency:` block keyed on `inputs.concurrencyKey` with `cancel-in-progress: false` so two simultaneous dispatches for the same test produce one queued heal, not two parallel runs (DET-07 SC #2 verifiable)"
    - "The workflow's `workflow_dispatch.inputs:` block declares the new flat schema from Plan 01 (`concurrencyKey` REQUIRED; `flakeRate`, `windowDays`, `runCount` optional)"
    - "An assertion-class fixture test exists at `fixture/tests/broken-assertion.spec.ts` whose root cause is a wrong assertion (NOT a selector bug); the e2e workflow can dispatch against it and verify a healer PR with `fixClass: 'assertions'` lands"
    - "The selector heal demo path from 03.1 still works end-to-end with full gates engaged: `skip_diff_lint: 'false'` AND `skip_post_fix_validation: 'false'` (or false-by-default once propagated). The demo workflow's `skip_post_fix_validation: 'true'` line is removed; only `skip_deterministic_check: 'true'` remains since post-fix validation now empirically passes."
    - "A maintainer can run `gh workflow run e2e-heal-self.yml` twice in rapid succession for the same test and observe via `gh run list` that the second run is queued, not parallel (manual UAT — automated assertion is impractical)"
  artifacts:
    - path: ".github/workflows/e2e-heal-self.yml"
      provides: "Updated workflow with concurrency block + 8-flat-input schema + assertion-class dispatch flag"
    - path: "fixture/tests/broken-assertion.spec.ts"
      provides: "Assertion-class fixture: wrong assertion text against a known-correct rendered state"
  key_links:
    - from: ".github/workflows/e2e-heal-self.yml (workflow_dispatch.inputs)"
      to: "src/healer/dispatch-payload.ts (DispatchPayload schema)"
      via: "workflow input keys mirror the Zod schema 1:1"
      pattern: "concurrencyKey"
    - from: ".github/workflows/e2e-heal-self.yml (concurrency.group)"
      to: "src/ingest/dispatch.ts (buildConcurrencyKey)"
      via: "the dispatched concurrencyKey input flows directly into the group expression"
      pattern: "playwright-healer-.*github.repository.*inputs.concurrencyKey"
---

<objective>
End-to-end verification: prove that Plans 01-04 work together on a real GitHub Actions runner. Specifically:

1. Add the `concurrency:` block to `e2e-heal-self.yml` keyed on the new `concurrencyKey` input (DET-07 SC #2 verification — the runtime block lives in the workflow file, not the action, per RESEARCH §"Anti-Patterns to Avoid").
2. Update the `workflow_dispatch.inputs:` block to match the 8-flat-input schema from Plan 01 (adds `concurrencyKey` REQUIRED + `flakeRate`/`windowDays`/`runCount` optional).
3. Add an assertion-class fixture test (`fixture/tests/broken-assertion.spec.ts`) and a job in `e2e-heal-self.yml` that dispatches against it with `fixClassHint: 'assertions'` to verify Plan 02's prompt template + classifier work end-to-end.
4. Re-engage the post-fix validation gate (drop `skip_post_fix_validation: 'true'` from the workflow) and confirm the existing 03.1 selector heal still lands a PR — this is the empirical check from RESEARCH §"Diff-Lint Compatibility Verification" generalizing to validate gates.
5. Manual UAT: a maintainer dispatches the workflow twice in rapid succession (same test) and verifies via `gh run list` that the second run is QUEUED, not parallel.

Purpose: closes Phase 04 success criteria #2 (concurrency group queues simultaneous dispatches) and #3 (a fixture test whose root cause is an assertion triggers an `assertions` fix).

Output: updated workflow YAML; new assertion-class fixture; post-fix validation re-engaged in the demo path; documented manual-UAT runbook for the concurrency check.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-auto-dispatch-full-fix-classes-deduplication/04-CONTEXT.md
@.planning/phases/04-auto-dispatch-full-fix-classes-deduplication/04-RESEARCH.md
@.planning/phases/04-auto-dispatch-full-fix-classes-deduplication/04-PATTERNS.md
@.github/workflows/e2e-heal-self.yml
@fixture/tests/broken-selector.spec.ts
@src/healer/dispatch-payload.ts
@src/ingest/dispatch.ts

<interfaces>
<!-- Concurrency block recipe from RESEARCH §Pattern 2 (no in-repo analog — workflow file does not currently declare concurrency) -->

```yaml
on:
  workflow_dispatch:
    inputs:
      commitSha:      { required: true }   # was already present
      testFile:       { required: true }   # was already present
      testTitle:      { required: true }   # was already present
      fixClassHint:   { required: true }   # was already present
      flakeRate:      { required: false }  # NEW Phase 04
      windowDays:     { required: false }  # NEW Phase 04
      runCount:       { required: false }  # NEW Phase 04
      concurrencyKey: { required: true }   # NEW Phase 04 — required from now on

concurrency:
  group: playwright-healer-${{ github.repository }}-${{ inputs.concurrencyKey }}
  cancel-in-progress: false
```

Existing `e2e-heal-self.yml` block at lines 18-36 — current dispatch inputs (selectors-era, post-Plan-01 will be widened):
```yaml
on:
  workflow_dispatch:
    inputs:
      testFile:       { required: false, default: 'fixture/tests/broken-selector.spec.ts' }
      testTitle:      { required: false, default: 'clicks submit button and sees confirmation' }
      fixClassHint:   { required: false, default: 'selectors' }
      commitSha:      { required: false, default: '' }
```

Default values: keeping them is fine for manual maintainer dispatch convenience, but `concurrencyKey` MUST be required since Plans 01-04 always populate it.

Existing `e2e-heal-self.yml` skip flags at L137-141:
```yaml
          skip_deterministic_check: 'true'    # KEEP — broken fixture is deterministic by design
          skip_post_fix_validation: 'true'    # REMOVE — re-engage post-fix validation
          skip_diff_lint: 'false'              # KEEP as explicit (default is now also 'false' per Plan 04)
```

Existing fixture file `fixture/tests/broken-selector.spec.ts` (the analog for the new assertion fixture). Read its source for exact shape — typical Playwright spec with `test('clicks submit button and sees confirmation', ...)`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update e2e-heal-self.yml workflow_dispatch inputs + concurrency block + drop skip_post_fix_validation</name>
  <files>.github/workflows/e2e-heal-self.yml</files>
  <read_first>
    - .github/workflows/e2e-heal-self.yml (full — 175 lines)
    - .planning/phases/04-auto-dispatch-full-fix-classes-deduplication/04-RESEARCH.md (sections "Pattern 2: Concurrency Group Slug+Hash", "Open Questions §4 — diff-lint re-engagement default flip", "Pitfall 5")
    - src/healer/dispatch-payload.ts (post-Plan-01 — schema is the source of truth for input names + types)
  </read_first>
  <action>
    Modify `.github/workflows/e2e-heal-self.yml` in-place:

**Part A — extend `workflow_dispatch.inputs` block (lines 18-36)**:

```yaml
on:
  workflow_dispatch:
    inputs:
      # Existing (Phase 03 era) — defaults retained for maintainer dispatch convenience
      testFile:
        description: 'Test file path (relative to repo root)'
        required: false
        default: 'fixture/tests/broken-selector.spec.ts'
      testTitle:
        description: 'Test title (matches a Playwright test() name)'
        required: false
        default: 'clicks submit button and sees confirmation'
      fixClassHint:
        description: 'Fix class hint: selectors | waits | assertions | slow'
        required: false
        default: 'selectors'
      commitSha:
        description: 'Commit SHA to heal against (blank → HEAD of main)'
        required: false
        default: ''
      # NEW Phase 04 — flat run-stats (optional; ingest auto-dispatch always populates)
      flakeRate:
        description: 'Recent flake rate (0.0–1.0). Optional for manual dispatch.'
        required: false
        default: ''
      windowDays:
        description: 'Rolling window in days. Optional for manual dispatch.'
        required: false
        default: ''
      runCount:
        description: 'Recent run count. Optional for manual dispatch.'
        required: false
        default: ''
      # NEW Phase 04 — DET-07 concurrency key (REQUIRED from now on; manual maintainer
      # dispatch must compute it via slug(testFile)+slug(testTitle)+sha1(testFile::testTitle).
      # Default value here is a sentinel so a maintainer can hand-dispatch the broken-selector
      # fixture without computing the hash; ingest's auto-dispatch always passes the real value.
      concurrencyKey:
        description: 'Slug+SHA1 concurrency key per (testFile, testTitle). Compute via src/ingest/dispatch.ts buildConcurrencyKey for ingest-fired dispatches.'
        required: true
        default: 'manual-broken-selector-default'
```

NOTE: `default: 'manual-broken-selector-default'` makes the input still hand-dispatchable while satisfying the schema's `concurrencyKey: z.string().min(1)` requirement.

**Part B — add `concurrency:` block at the workflow level** (top-level, alongside `on:` and `permissions:`):

```yaml
permissions:
  contents: read

concurrency:
  group: playwright-healer-${{ github.repository }}-${{ inputs.concurrencyKey }}
  cancel-in-progress: false
```

CRITICAL: place this AFTER the `permissions:` block at line 38-39 and BEFORE the `jobs:` block at line 41. The `concurrency:` block at workflow level applies to every job in the file. `cancel-in-progress: false` matches CONTEXT D-03 ("queue, don't cancel — we want both runs' detection evidence preserved if they raced"). Pitfall 5: GitHub treats group names case-insensitively; `buildConcurrencyKey` already lowercases the slug component, and the SHA-1 hash preserves case-distinct uniqueness for variant titles.

**Part C — drop `skip_post_fix_validation: 'true'` from the heal job** (lines 137-141 area):

```yaml
          # Demo-mode flags inherited from Phase 03.1 — UPDATED Phase 04:
          # post-fix validation re-engaged (RESEARCH §"Diff-Lint Compatibility
          # Verification" empirically confirms the existing selector heal still
          # passes the validator). Deterministic check stays skipped because the
          # broken-fixture is deterministic by design (the bug is intentional).
          skip_deterministic_check: 'true'
          # skip_post_fix_validation: 'true'   ← REMOVED Phase 04
          skip_diff_lint: 'false'
```

Comment out (do not delete) the `skip_post_fix_validation` line so a maintainer rolling back to demo behavior has a one-line revert path.

**Part D — additional tweaks for the test infrastructure (optional, surface in SUMMARY)**:

The existing workflow has 3 jobs: `assert-test-broken`, `heal`, `assert-pr-opened`. The new `concurrency:` block applies to all three. This is correct behavior — if two dispatches race, both queue at job-graph level, and the second waits for the first's PR-creation step before starting the red-guard job.

Do NOT add the assertion-class dispatch as a NEW job in this workflow — keep e2e-heal-self.yml focused on a single dispatch shape (Task 3 either adds a sibling workflow file or extends with a matrix). Surface the design choice in the SUMMARY for Plan 05.
  </action>
  <verify>
    <automated>grep -n 'concurrency:' .github/workflows/e2e-heal-self.yml && grep -n 'concurrencyKey:' .github/workflows/e2e-heal-self.yml && ! grep -nE "^\\s+skip_post_fix_validation: 'true'" .github/workflows/e2e-heal-self.yml</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "^concurrency:" .github/workflows/e2e-heal-self.yml` returns 1 (top-level block exists).
    - `grep -n "group: playwright-healer-" .github/workflows/e2e-heal-self.yml` returns one match referencing `inputs.concurrencyKey`.
    - `grep -n "cancel-in-progress: false" .github/workflows/e2e-heal-self.yml` returns one match.
    - `grep -nE "concurrencyKey:" .github/workflows/e2e-heal-self.yml` returns ≥2 matches (input declaration + concurrency expression).
    - `grep -nE "^\\s+skip_post_fix_validation: 'true'" .github/workflows/e2e-heal-self.yml` returns ZERO matches (or one match commented out — `#`-prefixed).
    - YAML parses cleanly: `python3 -c "import yaml; yaml.safe_load(open('.github/workflows/e2e-heal-self.yml'))"` exits 0 (or use `yq` if available).
  </acceptance_criteria>
  <threat_ref>T-04-04 (concurrencyKey injection — input default is a constant string; ingest auto-dispatch passes the slug+sha1 result; both flow through `${{ inputs.concurrencyKey }}` GitHub Actions expression which sanitizes per the runner's group-name rules. Empty/null protected via `required: true`.)</threat_ref>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add assertion-class fixture (fixture/tests/broken-assertion.spec.ts)</name>
  <files>fixture/tests/broken-assertion.spec.ts</files>
  <read_first>
    - fixture/tests/broken-selector.spec.ts (full — the analog; mirror its structure exactly, swap selector bug for assertion bug)
    - fixture/index.html (or main fixture HTML — to know what text actually renders so the assertion bug is realistic)
    - .planning/phases/04-auto-dispatch-full-fix-classes-deduplication/04-PATTERNS.md (section "NEW fixture/tests/broken-assertion.spec.ts")
  </read_first>
  <behavior>
    - Test 1 (the fixture is genuinely broken): Running `npx playwright test fixture/tests/broken-assertion.spec.ts` exits non-zero — the assertion fails because the rendered text is `"Submitted"` (no `!`) and the test asserts `.toHaveText('Submitted!')` (with `!`).
    - Test 2 (the bug is assertion-class, NOT selector-class): The selector resolves correctly (locator finds the element); the assertion against the resolved element's text is what fails. The agent should classify this as `'assertions'` per RESEARCH §"FIX-07 Architecture" classifier rules (`expect(received).` / `Expected:..Received:` substring → 'assertions').
    - Test 3 (the bug is fixable by a strengthened-not-loosened assertion): A correct fix is `.toHaveText('Submitted')` (drop the bang) OR `.toContainText('Submit')` — both stronger or equivalent in semantic precision; neither weakens the test.
  </behavior>
  <action>
    Read `fixture/tests/broken-selector.spec.ts` to know the exact import + setup shape. Then create a sibling file with the assertion bug:

```typescript
// fixture/tests/broken-assertion.spec.ts
//
// Phase 04 FIX-07 fixture: assertion-class bug. The submit button's selector
// resolves correctly, but the test asserts against the wrong rendered text —
// `'Submitted!'` (with bang) when the page actually renders `'Submitted'`.
//
// Expected agent classification: `assertions` (errorSignature contains
// `expect(received).toHaveText` plus `Expected: "Submitted!"` / `Received: "Submitted"`).
// Expected fix: relax the literal to match actual rendered text — `.toHaveText('Submitted')`
// or `.toContainText('Submit')`. Both are stronger-or-equivalent assertions; the
// diff-lint pass does NOT classify these as weakening (they correct the expected
// value rather than swap to a less-specific matcher).

import { test, expect } from '@playwright/test';

test('clicks submit button and sees assertion confirmation', async ({ page }) => {
  // BASE_URL is set by the e2e-heal-self.yml workflow; default for local dev
  await page.goto(process.env.BASE_URL ?? 'http://localhost:8080');

  // Selector RESOLVES correctly — this is NOT a selector bug.
  await page.getByRole('button', { name: 'Submit' }).click();

  // ASSERTION BUG: actual rendered text is "Submitted" (no bang).
  // The fix-class hint should be `assertions`, not `selectors`.
  await expect(page.locator('#message')).toHaveText('Submitted!');
});
```

CRITICAL: read the actual fixture HTML (likely `fixture/index.html` or wherever `npm run start` serves) BEFORE finalizing the literal `'Submitted!'` — confirm the page actually renders `'Submitted'` without the bang. If the HTML renders something different, adjust the assertion to mismatch reality (the bug is "assertion expects X, page renders Y" — both halves must be real).

If the fixture HTML's actual rendered text is `'Submission complete'` or some other string, use that as the page-reality and craft the test to assert a different specific string. The contract is: agent must observe actual page state and correct the literal in the test, NOT change the selector.

DO NOT modify `fixture/index.html` — keep the rendered text alone; the test is the bug.

Verify locally (developer environment): run `cd fixture && npm install && npx playwright install chromium && npm run start &` then `npx playwright test fixture/tests/broken-assertion.spec.ts` and confirm it FAILS with an error message containing `Expected: "Submitted!"` and `Received: "Submitted"` (or whatever the actual rendered text is). If it fails for a different reason (selector not resolving, app not starting), the fixture HTML may need a small adjustment OR the test selector needs to match the actual fixture markup.
  </action>
  <verify>
    <automated>cd fixture && npx playwright test tests/broken-assertion.spec.ts --reporter=list 2>&1 | grep -E 'Expected:|expect\\(received\\)|toHaveText' && exit 1 || exit 0</automated>
  </verify>
  <acceptance_criteria>
    - `fixture/tests/broken-assertion.spec.ts` exists and is a valid Playwright spec.
    - The test FAILS when run against the fixture app — exit code is non-zero (this is GOOD: a passing fixture test would mean the bug isn't actually broken).
    - The Playwright failure message contains `expect` and either `toHaveText` or `toContainText` (verifies the assertion-class shape per the classifier's substring rules).
    - The selector (`getByRole('button', { name: 'Submit' })` or whatever resolves in the fixture HTML) successfully locates the element — failure is at the assertion, NOT at locator resolution. Verify via the Playwright trace or console output: the failure should be on the `expect(...)` line, not on the `click()` line.
    - The test title `'clicks submit button and sees assertion confirmation'` does NOT collide with the existing broken-selector test's title `'clicks submit button and sees confirmation'` (concurrency-key uniqueness — verifies they get different keys).
  </acceptance_criteria>
  <threat_ref>T-04-04 (the agent-controlled fix path now exercises the assertions class — the strengthened-not-loosened constraint is enforced by `lintDiff()` at runtime AND by the `assertions-no-trace.md` prompt's "Forbidden" stanza. This fixture is the empirical check that those gates work for a real assertion bug.)</threat_ref>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Manual UAT — concurrency-block queue verification + assertion-class heal</name>
  <what-built>
    Plans 01-04 ship the auto-dispatch wiring, FIX-07 cascade, PRI-04 dedup, and heal-cap. Task 1 here adds the runtime `concurrency:` block to `e2e-heal-self.yml`. Task 2 ships an assertion-class fixture. The remaining verification is empirical and requires GitHub Actions runtime to evaluate concurrency — no automated assertion is feasible from local vitest.
  </what-built>
  <how-to-verify>
    Sequence the maintainer through these checks (this checkpoint pauses for human input — provide a `gh run list` paste or a screenshot URL when each step is done).

    **STEP A — selector heal still works with full gates engaged.**

    Pre-flight: confirm `fixture/tests/broken-selector.spec.ts` is in its broken form (locator `#wrong-id`). If a previous heal PR was merged, revert it manually first.

    1. Push the Phase 04 branch to GitHub.
    2. Run: `gh workflow run e2e-heal-self.yml -F testFile='fixture/tests/broken-selector.spec.ts' -F testTitle='clicks submit button and sees confirmation' -F fixClassHint='selectors' -F concurrencyKey='manual-selector-uat-1'`
    3. Watch with: `gh run watch` (or visit the Actions tab).
    4. EXPECTED: all three jobs (`assert-test-broken`, `heal`, `assert-pr-opened`) green; a healer PR opens with `fixClass: selectors` and the post-fix validation reports actual passes (NOT "skipped").

    Reply with: the resulting PR URL + a snippet of the Actions step summary showing the validation pass-rate (NOT "skipped").

    **STEP B — assertion-class heal lands.**

    1. Run: `gh workflow run e2e-heal-self.yml -F testFile='fixture/tests/broken-assertion.spec.ts' -F testTitle='clicks submit button and sees assertion confirmation' -F fixClassHint='assertions' -F concurrencyKey='manual-assertion-uat-1'`
    2. EXPECTED: `assert-test-broken` (red guard) fails as expected (the test is broken); `heal` job invokes the action, the agent classifies as `assertions` (or accepts the hint), produces a diff that corrects the assertion text without weakening it; `assert-pr-opened` confirms a PR titled `[playwright-healer] Fix flaky clicks submit button and sees assertion confirmation`.
    3. Inspect the PR diff to confirm: (a) `fixClass: assertions` in the PR body; (b) the diff modifies the assertion literal (e.g., `'Submitted!'` → `'Submitted'`) WITHOUT swapping `.toHaveText` to `.toContainText` weakly OR removing the assertion.

    Reply with: the PR URL and the diff snippet.

    **STEP C — concurrency-group queue verification (DET-07 SC #2).**

    1. Run two dispatches for the SAME test in rapid succession (within ~2 seconds):
       ```
       gh workflow run e2e-heal-self.yml -F testFile='fixture/tests/broken-selector.spec.ts' -F testTitle='clicks submit button and sees confirmation' -F fixClassHint='selectors' -F concurrencyKey='uat-c-key' &
       gh workflow run e2e-heal-self.yml -F testFile='fixture/tests/broken-selector.spec.ts' -F testTitle='clicks submit button and sees confirmation' -F fixClassHint='selectors' -F concurrencyKey='uat-c-key' &
       wait
       ```
    2. Run: `gh run list --workflow=e2e-heal-self.yml --limit 5 --json status,conclusion,createdAt,databaseId,event`
    3. EXPECTED: TWO runs visible; ONE is `status: in_progress` (or `completed`), the OTHER is `status: queued`. The queued run does NOT execute concurrently with the first — it waits.
    4. After the first run completes, the second run transitions from `queued` → `in_progress` automatically.

    Reply with: the JSON output from `gh run list` (paste the relevant 2 entries) showing the QUEUE state.

    **STEP D — heal-cap verification (DET-07 sticky cap).**

    Optional but recommended: after Steps A-C have produced 3+ heal events (visible in `runs/YYYY/MM/DD-heals.ndjson` on the `playwright-healer-state` branch), trigger a 4th dispatch for the same testId. EXPECTED: ingest-side D-04 cap-hit OR healer-side Guard 3 backstop fires; a `cap-exceeded` issue opens; no new PR is created.

    Reply: the cap-exceeded issue URL.
  </how-to-verify>
  <resume-signal>
    Type "approved" if all four steps (A: selector + full gates green; B: assertion-class PR landed; C: concurrency queue confirmed via `gh run list`; D: cap-exceeded issue if applicable) succeed.

    If any step fails:
    - "fail step A: <evidence>" — selector heal regression with full gates engaged. Likely cause: the validator runs against a different environment than fixture-ci.yml. Diagnose via the Actions log.
    - "fail step B: <evidence>" — assertion-class heal didn't produce the expected fix shape. Likely cause: classifier rule misses, prompt template gap, OR the agent weakened an assertion. Inspect the agent's JSON proposal in the run log.
    - "fail step C: <evidence>" — concurrency block isn't queueing. Likely cause: group expression syntax error or `cancel-in-progress: true` slipped in. Inspect `e2e-heal-self.yml` and the failing run's "Setup job" expansion.
    - "fail step D: <evidence>" — cap-bypass detected. Likely cause: heal-events not being written; OR Guard 3 worktree bootstrap is failing silently.
  </resume-signal>
  <threat_ref>T-04-02 (manual UAT confirms the cap fires end-to-end on a live runner — the most reliable check that the dual-gate defense-in-depth is wired correctly. T-04-04 verified empirically: the assertion-class fixture proves the agent doesn't weaken assertions in practice.)</threat_ref>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dispatch input → workflow concurrency.group expression | `${{ inputs.concurrencyKey }}` — runner expression evaluation; case-insensitive group name matching |
| fixture HTML rendered text → assertion-class agent prompt | Agent observes via Playwright MCP; reproducing the failure live is the trust anchor |
| manual UAT human → workflow inputs | Maintainer-typed values; standard `gh workflow run` audit log via GitHub Actions |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-04 | Tampering | `concurrencyKey` workflow input | mitigate | `required: true` rejects empty; `${{ inputs.concurrencyKey }}` is rendered as a string into the group name; case-insensitivity (Pitfall 5) handled upstream by `buildConcurrencyKey` lowercasing the slug + SHA-1-distinguishing case-variant titles. |
| T-04-04 | Tampering | broken-assertion fixture | accept | Fixture lives in this repo's own test directory; it's NOT consumer-shipped code. Worst case: a malicious fixture causes the agent to propose a weakened assertion → diff-lint blocks → issue-fallback. Defense-in-depth holds. |
| T-04-02 | Denial of Service | manual UAT cap-hit verification | mitigate | The cap-hit step is a positive verification of the rate-limiting; it confirms the gate exists. Maintainer cannot accidentally bypass via UAT — the cap is per-test, the test is the fixture, and the fixture is read-only. |
</threat_model>

<verification>
After all three tasks:
- Workflow YAML parses cleanly (Python or yq).
- `concurrency:` block present at workflow level with `cancel-in-progress: false`.
- `concurrencyKey` is required input.
- New assertion fixture exists and fails (red-guard self-test).
- Manual UAT all four steps approved by the maintainer (Task 3 is a checkpoint:human-verify gate).
- The end-to-end runs land:
  - One PR for the selectors heal with full gates engaged (validation pass-rate honest).
  - One PR for the assertions heal proving FIX-07 works for a real assertion bug.
  - One queued run during the rapid-double-dispatch (DET-07 SC #2).
  - One `cap-exceeded` issue after 3+ heal events accumulate (DET-07 sticky-cap).
</verification>

<success_criteria>
**Phase 04 complete when**:
- ROADMAP Phase 4 SC #1 (auto-dispatch fires from threshold breach) — empirically demonstrated in Plan 01 unit tests + Step A of the UAT.
- ROADMAP Phase 4 SC #2 (concurrency block queues simultaneous dispatches) — UAT Step C.
- ROADMAP Phase 4 SC #3 (assertion-class fix triggers from real assertion bug) — UAT Step B.
- ROADMAP Phase 4 SC #4 (re-trigger comments on existing PR/issue, no duplicate) — Plan 03 unit tests; can be additionally verified manually by re-dispatching a recently-healed test.

**Plan 05 specifically closes**:
- DET-07 (concurrency block in workflow file).
- FIX-07 e2e validation (assertions class works on a live runner with a live LLM).
- The "re-run 03.1 demo with full gates on" verification from CONTEXT specifics line 116.
</success_criteria>

<output>
After completion, create `.planning/phases/04-auto-dispatch-full-fix-classes-deduplication/04-05-SUMMARY.md`. Include:
- The updated `e2e-heal-self.yml` shape (concurrency block + 8-flat-input schema + post-fix validation re-engaged).
- The new assertion-class fixture file path + observed rendered text vs assertion mismatch.
- Manual UAT outcomes for Steps A-D (or notes on which failed and what was diagnosed).
- Phase 04 close-out checklist marking all 4 ROADMAP success criteria with evidence URLs.
</output>
