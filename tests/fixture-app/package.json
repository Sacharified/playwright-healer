{
  "name": "playwright-healer-fixture",
  "private": true,
  "version": "0.0.0",
  "description": "In-repo fixture app for playwright-healer self-test (tests/fixture-app/)",
  "scripts": {
    "start": "http-server -p 8080 -c-1 -s",
    "test": "playwright test"
  },
  "devDependencies": {
    "@playwright/test": "^1.50.0",
    "http-server": "^14.1.1"
  }
}
